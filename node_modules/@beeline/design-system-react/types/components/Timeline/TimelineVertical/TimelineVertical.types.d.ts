import type { TimelineProps } from '../Timeline.types';
export interface TimelineVerticalProps extends Pick<TimelineProps, 'steps' | 'completed' | 'collapsable' | 'collapsed' | 'onCollapse'> {
    activeStepIndex?: number;
}
