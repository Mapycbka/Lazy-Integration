import type { AtomPosition, StageType, TimelineIconType, TimelineProps } from '../Timeline.types';
type TimelinePropsType = Pick<TimelineProps, 'direction'>;
export interface StyledIconProps {
    type?: TimelineIconType;
}
export interface ContainerProps extends Pick<TimelineProps, 'collapsable' | 'collapsed'> {
    position?: AtomPosition;
}
export interface TimelineAtomProps extends StyledIconProps, ContainerProps, TimelinePropsType, Pick<StageType, 'iconName'> {
}
export {};
