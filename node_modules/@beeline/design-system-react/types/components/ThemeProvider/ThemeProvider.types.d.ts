import type { ReactNode } from 'react';
export interface ThemeProviderProps {
    children: ReactNode;
    /**
     * название темы
     */
    theme: ThemeType;
    /**
     * маркер необходимости применить тему глобально
     * если передан - токены темы будут применены к рутовому элементу ({@link ThemeProviderProps.rootSelector})
     */
    isRoot?: boolean;
    /**
     * селектор корневого элемента. По умолчанию body
     */
    rootSelector?: string;
    className?: string;
}
export type ThemeType = 'light' | 'dark';
