import type { ReactNode } from 'react';
export interface ModalProps {
    open?: boolean;
    className?: string;
    /**
     * Идентификатор компонента, в который будет смонтирован Dialog.
     * По умолчанию root.
     */
    applicationRootElement?: string;
    children: ReactNode;
}
