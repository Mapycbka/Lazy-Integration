import type { DropdownMenuProps } from '../DropdownMenu.types';
/**
 * Интерфейс пропсов компонента Positioner.
 */
export interface PositionerProps extends Omit<DropdownMenuProps, 'dropdownElementID' | 'onOutsideClick' | 'className'> {
}
/**
 * Интерфейс стилей компонента Positioner.
 */
export interface PositionerStyle {
    /**Координаты позиционера снизу.*/
    bottom?: number | string;
    /**Координаты позиционера слева.*/
    left?: number | string;
    /**Тип позиционирования.*/
    position?: 'absolute' | 'fixed';
    /**Координаты позиционера справа.*/
    right?: number | string;
    /**Координаты позиционера сверху.*/
    top?: number | string;
    /**Высота позиционера.*/
    height?: number | string;
}
