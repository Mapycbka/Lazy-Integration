import type { Icons } from '@beeline/design-tokens/js/iconfont/icons';
import type { ReactNode } from 'react';
export interface GroupItem {
    name: string;
    path: string;
    icon?: Icons;
    disabled?: boolean;
    /**
     * Отключает активное состояние для элемента
     */
    disableActive?: boolean;
    /**
     * URL для создания ссылки вместо кнопки
     */
    href?: string;
    /**
     * Атрибут target для ссылки
     */
    target?: string;
    /**
     * Атрибут rel для ссылки
     */
    rel?: string;
    /**
     * Состояние загрузки элемента
     */
    isLoading?: boolean;
    /**
     * Вложенные подразделы (максимум 2 уровня)
     */
    children?: GroupItem[];
}
export interface Groups {
    title?: ReactNode;
    items: GroupItem[];
}
interface NavigationDrawer extends React.ButtonHTMLAttributes<HTMLDivElement> {
}
export interface NavigationDrawerProps extends NavigationDrawer {
    className?: string;
    /**
     * список элементов основной группы
     */
    groups: Groups[];
    /**
     * Отображение заголовков групп элементов, по умолчанию true
     */
    isGroupTitle?: boolean;
    /**
     * Отображение разделителей групп элементов, по умолчанию true
     */
    isGroupDivider?: boolean;
    /**
     * текущий активный роут
     *
     * Можно передать чистую строку, тогда она будет активна по умолчанию.
     * При клике на новый айтем компонент самостоятельно изменит активный элемент
     * @example <NavigationDrawer active="default-page" />
     *
     * Можно передать переменную, при ее изменении активный роут также измениться
     * @example <NavigationDrawer active={activeRoute} />
     */
    active?: string;
    /**
     * список элементов футер группы
     */
    bottom?: Groups[];
    /**
     * функция возвращающая имя активного элемента при нажатие
     */
    onClickItem?: (name: string) => void;
    /**
     * Функция которая вызывается при клике на бекдроп в мобильной версии
     */
    mobileOnOutsideClick?: (event: any) => void;
    /**
     * Определяет состояние компонента: развернут/не развернут (по умолчанию false).
     *
     * Поведение зависит от режима работы:
     * - Неконтролируемый режим (без onOpen/onClose): isOpen используется только для инициализации
     * - Контролируемый режим (с onOpen/onClose): isOpen полностью управляет состоянием извне
     */
    isOpen?: boolean;
    /**
     * Callback, вызываемый при запросе закрытия компонента.
     * Наличие этого колбэка переводит компонент в контролируемый режим.
     */
    onClose?: () => void;
    /**
     * Callback, вызываемый при запросе открытия компонента.
     * Наличие этого колбэка переводит компонент в контролируемый режим.
     */
    onOpen?: () => void;
    /**
     * Отключает мобильную адаптацию компонента
     * @default false
     */
    disableMobileView?: boolean;
    /**
     * Состояние раскрытых вложенных пунктов меню (ключ — path родительского элемента).
     * Вместе с onExpandedItemsChange переводит в контролируемый режим.
     */
    expandedItems?: Record<string, boolean>;
    /**
     * Callback при изменении раскрытия вложенных пунктов.
     * Вместе с expandedItems переводит в контролируемый режим.
     */
    onExpandedItemsChange?: (expandedItems: Record<string, boolean>) => void;
    /**
     * Начальное состояние раскрытых пунктов в неконтролируемом режиме.
     */
    defaultExpandedItems?: Record<string, boolean>;
}
export {};
