import type { ReactNode } from 'react';
import React from 'react';
import type { Option } from './Select.types';
import { CheckboxType } from './Select.types';
/**
 * Сравнивает два значения с учетом compareBy
 */
export declare const compareValues: <T>(a: T, b: T, compareBy?: string | ((a: T, b: T) => boolean)) => boolean;
export declare const getPreparedOptions: <T extends {
    value?: string;
}>(options: Array<Option<T>>, filter?: boolean, filterValue?: string, filterFunction?: (options: Array<Option<T>>, filterValue: string) => Array<Option<T>>) => Array<Option<T>>;
/**
 * Возвращает опции в порядке отображения при группировке (как в Options).
 * Без groups возвращает options как есть.
 */
export declare const getOptionsInDisplayOrder: <T>(options: Array<Option<T>>, groups?: Array<Option<string>>) => Array<Option<T>>;
export declare const defaultRenderValue: <T>(values: T[]) => string;
export declare const defaultMakeOption: <T>(option: T, showTooltip: boolean) => React.JSX.Element;
export declare const getLoading: (loadingContent: string | React.ReactNode) => ReactNode;
export declare const getCheckboxStatus: <T>(options: Array<Option<T>>, selectedValues: Array<Option<T>>, filterValue: string, filter: boolean, filterFunction: (options: Array<Option<T>>, filterValue: string) => Array<Option<T>>, compareBy?: string | ((a: T, b: T) => boolean)) => CheckboxType;
