import './Options.scss';
import type { Option } from ':components/Select/Select.types';
import { CheckboxType } from ':components/Select/Select.types';
import React from 'react';
export interface OptionsProps<T> {
    selectedItems: Array<Option<T>>;
    optionsData: Array<Option<T>>;
    selectAll: boolean;
    filter: boolean;
    filterValue: string;
    filterFunction: (options: Array<Option<T>>, filterValue: string) => Array<Option<T>>;
    selectAllIndex: number;
    optionsClassName: string;
    multiple: boolean;
    itemClassName: string;
    onMultiselectItem: (item: Option<T>) => () => void;
    onManualClickOption: (option: T) => void;
    onSelectItem: (item: Option<T>) => () => void;
    focusedIndex: number;
    handleSelectAll: () => void;
    makeOption: (option: T, selected?: boolean) => React.JSX.Element;
    showTooltip: boolean;
    defaultMakeOption: <T>(option: T, showTooltip: boolean) => React.JSX.Element;
    selectAllText: string;
    loading: boolean;
    loadingText: React.ReactNode;
    loadingContent: string | React.ReactNode;
    noOptionsText: string;
    optionsRef: React.MutableRefObject<HTMLDivElement>;
    selectStatus: CheckboxType;
    makeOptionWrapper?: (option: T, selected?: boolean, defaultProps?: {
        key: string | number;
        className: string;
        onClick: () => void;
        children: React.JSX.Element;
    }) => React.JSX.Element;
    groups?: Array<Option<string>>;
    dataTestId: string;
}
export declare const Options: <T>({ selectedItems, optionsData, selectAll, filter, filterValue, filterFunction, selectAllIndex, optionsClassName, multiple, itemClassName, onMultiselectItem, onManualClickOption, onSelectItem, focusedIndex, handleSelectAll, makeOption, showTooltip, defaultMakeOption, selectAllText, loading, loadingText, loadingContent, noOptionsText, optionsRef, selectStatus, makeOptionWrapper, groups, dataTestId, }: OptionsProps<T>) => React.JSX.Element;
