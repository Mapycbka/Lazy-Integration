import './Item.scss';
import type { Option } from ':components/Select/Select.types';
import { CheckboxType } from ':components/Select/Select.types';
import React from 'react';
interface ItemProps<T> {
    optionId?: string;
    option: Option<T>;
    isSelected: CheckboxType;
    selectAll: boolean;
    multiple: boolean;
    itemClassName: string;
    onMultiselectItem: (item: Option<T>) => () => void;
    onManualClickOption: (option: T) => void;
    onSelectItem: (item: Option<T>) => () => void;
    /** focusedIndex – текущий индекс, выставленный хукoм клавиатурной навигации */
    focusedIndex: number;
    /** optionIndex – индекс текущей опции в списке */
    optionIndex: number;
    handleSelectAll: () => void;
    makeOption?: (option: T, selected?: boolean) => React.JSX.Element;
    showTooltip: boolean;
    defaultMakeOption<T>(option: T, showTooltip: boolean): React.JSX.Element;
    /** Кастомный компонент для обертки элемента */
    makeOptionWrapper?: (option: T, selected?: boolean /** Стандартные пропсы компонента */, defaultProps?: {
        key: string | number;
        className: string;
        onClick: () => void;
        children: React.JSX.Element;
    }) => React.JSX.Element;
}
export declare const Item: <T>({ optionId, option, isSelected, multiple, selectAll, itemClassName, onMultiselectItem, onManualClickOption, onSelectItem, focusedIndex, optionIndex, handleSelectAll, makeOption, showTooltip, defaultMakeOption, makeOptionWrapper, }: ItemProps<T>) => React.JSX.Element | null;
export {};
