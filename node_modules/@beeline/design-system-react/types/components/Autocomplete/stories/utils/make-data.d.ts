declare class Person {
    title: string;
    constructor(inputValue: string);
    id: string;
    subtitle: string;
}
export declare const makeData: (count: number, inputValue: any) => Person[];
export {};
