import type { Placement } from '@floating-ui/react';
import type { ReactNode } from 'react';
import type { TestComponentProps } from '../../types/test-component';
import type { SearchProps } from '../Search';
import type { Option } from '../Select';
import type { TextFieldProps } from '../TextField';
type SearchPropsOmit = Omit<SearchProps, 'onChange' | 'value'>;
type TextFieldPropsOmit = Omit<TextFieldProps, 'onChange' | 'value' | 'size' | 'type' | 'startAdornment' | 'prefix' | 'suffix'>;
interface PartialSelectProps<T> {
    /**
     * Колбек, вызываемый при выборе элемента меню.
     * @param {Option<T>} value Выбранное значение
     */
    onChange: (value: Option<T>) => void;
    /**
     * Колбек, вызываемый при закрытии компонента
     */
    onClose?: () => void;
    /**
     * Колбек, вызываемый при открытии компонента
     */
    onOpen?: () => void;
    /**
     * Если true, компонент отображается в открытом состоянии
     */
    open?: boolean;
    /**
     * Доступные опции меню
     */
    options: Array<Option<T>>;
    /**
     * Выбранная опция
     */
    value: Option<T> | null;
    /**
     * ID группы, которая должна отображаться по умолчанию
     */
    defaultGroupID?: string;
    /**
     * Класс для dropdown меню
     */
    dropdownClassName?: string;
    /**
     * ID для создаваемого dropdown меню
     */
    dropdownElementID?: string;
    /**
     * ID элемента, в который будет монтироваться dropdown меню
     */
    applicationRootElementID?: string;
    /**
     * Функция преобразования объекта опции в строку для отображения в поле
     * Рекомендуется оборачивать в useCallback для избежания лишних ререндеров
     */
    renderValue: (value: Option<T>) => string;
    /**
     * Колбек, вызываемый при изменении значения ввода
     */
    onInputChange: (inputValue: string) => void;
    /**
     * Колбек, вызываемый при очистке ввода
     */
    onInputClear: () => void;
    /**
     * Включить секцию группировки ввода
     */
    enableGrouping?: boolean;
    /**
     * Функция для кастомного отображения элементов опций
     */
    makeOption?: (option: Option<T>, inputValue: string) => React.JSX.Element | null;
    /**
     * Переключение режимов отображения компонента
     */
    type: 'search' | 'select';
    /**
     * @deprecated Используйте `noOptionsContent` вместо этого.
     * Содержимое, отображаемое, когда нет опций для отображения.
     */
    noOptionsText?: string | ReactNode;
    /**
     * Когда в поиске нету искомого, показывает кастомный текст или Node елемент
     */
    noOptionsContents?: string | ReactNode;
    loading?: boolean;
    /**
     * @deprecated
     * Будет удалено в следующих релизах, используйте loadingContent
     */
    loadingText?: string | ReactNode;
    loadingContent?: string | ReactNode;
    hideDropDown?: boolean;
    /**
     * Показывать тултип, если содержание польше чем размер поля (по умолчанию true).
     */
    showTooltip?: boolean;
    enableKeyboardNavigation?: boolean;
    maskConfig?: any;
    /**
     * Определяет, должно ли наложение блокировать прокрутку на теле документа.
     * @default true
     */
    overlayScroll?: boolean;
    /**
     * Указывает, нужно ли рендерить оверлей
     * */
    shouldRenderOverlay?: boolean;
    /**
     * Добавляет className для Overlay
     * */
    overlayClassName?: string;
    /**
     * Выравнивание дропдавна
     * */
    alignDropDown?: Placement;
    /**
     * Заголовок мобильного экрана
     * */
    mobileTitle?: string;
    /**
     * Текст над результатами в мобильной версии
     * */
    mobileCaption?: string;
    /**
     * Класс для модалки в мобильном варианте компонента
     * */
    classNameMobileModal?: string;
    disableMaxLength?: boolean;
}
export interface AutocompleteProps<T> extends SearchPropsOmit, TextFieldPropsOmit, PartialSelectProps<T>, TestComponentProps {
}
export {};
