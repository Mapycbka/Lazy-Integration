import type { InputHTMLAttributes, ReactNode } from 'react';
import type { TestComponentProps } from '../../types/test-component';
export type CheckboxType = 'checkbox' | 'indeterminate';
type OmittedInputPropsType = Omit<InputHTMLAttributes<HTMLInputElement>, 'onCopy' | 'onCopyCapture'>;
export interface CheckboxProps extends OmittedInputPropsType, TestComponentProps {
    /**
     * Заголовок
     */
    label?: string | ReactNode;
    /**
     * Состояние "отмеченный" / "не отмеченный"
     */
    checked?: boolean;
    /**
     * Тип иконки: "галка" или "чёрточка"
     */
    type?: CheckboxType;
    /**
     * Состояние "заблокирован" / "разблокирован"
     */
    disabled?: boolean;
    /**
     * Признак ошибки. Применяет визуальные стили ошибки и устанавливает aria-invalid.
     */
    error?: boolean;
    /**
     * Пользовательский класс
     */
    className?: string;
}
export {};
