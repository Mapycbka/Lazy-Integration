import type { HTMLAttributes } from 'react';
import { TestComponentProps } from 'src/types/test-component';
export interface FormLabelProps extends Omit<HTMLAttributes<HTMLLabelElement>, 'color'>, TestComponentProps {
    /**
     * TODO: Добавить функционал для обязательных полей.
     * [ЗАГЛУШКА] Указывает, является ли поле обязательным для заполнения.
     */
    required?: boolean;
    /**
     * Связывает label с элементом формы через id.
     */
    htmlFor?: string;
    /**
     * Содержимое label.
     */
    children: React.ReactNode;
}
