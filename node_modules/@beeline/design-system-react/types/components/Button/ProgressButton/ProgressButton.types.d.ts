import type { ButtonProps } from '../Button.types';
export type ProgressButtonColorVariants = 'primary' | 'secondary' | 'inverse' | 'danger' | 'accent-black' | 'accent-white'
/** @deprecated Use 'primary' instead */
 | 'contained'
/** @deprecated Use 'secondary' instead */
 | 'outlined'
/** @deprecated Use 'inverse' instead */
 | 'overlay';
export type LoadingStateTypes = 'default' | 'loading' | 'success' | 'error';
export interface ProgressButtonProps extends ButtonProps {
    /**
     * Тип прогресса определенный или неопределенный
     */
    determinateMode?: boolean;
    /** Значение прогресса в процентном отношении от 0 до 100 */
    progress?: number;
    /** Варианты отображения кнопки */
    variant?: ProgressButtonColorVariants;
    /**
     * задержка в финальном статусе (ошибка или успех)
     *
     * Время, которое кнопка будет находиться в финальном статусе, перед тем как сбросится до дефолтного
     *
     * Для того что бы финальный статус был бесконечным ставьте -1
     */
    statusDelayTime?: number;
    /**
     * обработчик нажатия на кнопку, асинхронная функция
     *
     * статусы кнопки повторят статус переданного обработчика
     */
    onClick?: () => Promise<unknown> | undefined;
    /**
     * Состояние кнопки
     */
    state?: LoadingStateTypes;
}
export interface SVGPathSettings {
    width: number;
    height: number;
    startPos: number;
    strokeWidth: number;
    borderRadius: number;
}
