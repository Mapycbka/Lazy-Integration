import type { SearchProps } from ':components/Search';
import type { ReactNode } from 'react';
import type { KeyLogosImgList } from 'src/constants/logosImgLists';
export interface HeaderProps {
    /** Имя продукта */
    nameProduct?: string;
    /** Выбрать нужное лого */
    nameLogo?: KeyLogosImgList;
    /** Передать своё лого */
    customLogo?: ReactNode;
    /** Добавляет пользовтельский контент в центр хедера*/
    children?: ReactNode | ReactNode[];
    /** Список иконок или других компонентов, которые мы хотим отрендерить в правой чати header*/
    iconsList?: ReactNode;
    /** Проброс пропсов для компонента Search, если обьект пустой -- рендера нету*/
    searchComponentProps?: SearchProps;
    nameProductRoutingComponent?: ReactNode;
    /** Кастомный роутинг компонент для nameLogo, для роутинга с customLogo - оберните customLogo в нужный вам компонент */
    nameLogoRoutingComponent?: ReactNode;
}
