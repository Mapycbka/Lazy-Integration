import type { ReactNode } from 'react';
import React from 'react';
export declare const renderChildren: (children: ReactNode | ReactNode[], isCollapsed: boolean, onClick: () => void) => React.JSX.Element[];
