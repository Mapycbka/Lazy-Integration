import type { BoxProps, BoxSpacing } from '../Box/Box.types';
export type StackDirection = 'vertical' | 'horizontal';
export type StackAlign = 'start' | 'center' | 'end' | 'stretch' | 'baseline';
export type StackJustify = 'start' | 'center' | 'end' | 'space-between' | 'space-around' | 'space-evenly';
export type StackWrap = 'nowrap' | 'wrap' | 'wrap-reverse';
interface StackOwnProps {
    /**
     * Направление расположения элементов.
     * По умолчанию `vertical` (column).
     */
    direction?: StackDirection;
    /**
     * Расстояние между элементами по токенам spacing.
     * По умолчанию `none`.
     */
    spacing?: BoxSpacing;
    /**
     * Выравнивание элементов по поперечной оси (`align-items`).
     * По умолчанию `stretch`.
     */
    align?: StackAlign;
    /**
     * Выравнивание элементов по главной оси (`justify-content`).
     * По умолчанию `start`.
     */
    justify?: StackJustify;
    /**
     * Управление переносом элементов (`flex-wrap`).
     * По умолчанию `nowrap`.
     */
    wrap?: StackWrap;
}
/**
 * Публичные пропсы `Stack`.
 */
export interface StackProps extends Omit<BoxProps, 'display'>, StackOwnProps {
}
export {};
