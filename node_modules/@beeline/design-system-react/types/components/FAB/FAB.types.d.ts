import type { Icons } from '@beeline/design-tokens/js/iconfont/icons';
import type { ButtonProps } from '../Button';
export type FABButtonType = 'extended' | 'standard' | 'mini';
export interface FABProps extends Omit<ButtonProps, 'type' | 'size' | 'variant' | 'endIcon' | 'startIcon'> {
    /**
     * Тип кнопки, влияет на форму и содержимое кнопки
     */
    type?: FABButtonType;
    /**
     * Название иконки.
     */
    iconName: Icons;
}
