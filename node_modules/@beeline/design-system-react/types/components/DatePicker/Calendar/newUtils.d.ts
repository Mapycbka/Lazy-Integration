import type { DateValue } from './Calendar.types';
/** Преобразование UTC->TZ. Если timeZone не задана, возвращаем копию originalUTC. */
export declare const convertUtcToTz: (originalUTC: Date, timeZone?: string) => Date;
/** Преобразование TZ->UTC. Если timeZone не задана, считаем, что zonedDate уже UTC. */
export declare const convertTzToUtc: (zonedDate: Date, timeZone?: string) => Date;
/** Проверка, что строка – это валидная ISO-дата (минимум "YYYY-MM-DD"). */
export declare const isValidDateString: (date: string) => boolean;
/** Превращаем ISO-строку в Date(UTC). Если некорректно, возвращаем null. */
export declare const getDateFromIsoString: (date: string) => Date | null;
export declare const isRangeMode: <T>(value: T | T[]) => value is T[];
/** Возвращаем Date(UTC) для value. Если не получилось, вернём new Date() (UTC now). */
export declare const getViewDate: (value: DateValue) => Date;
/** Форматируем Date(UTC) в нужном формате, напр. "yyyy-MM-dd'T'HH:mm:ss'Z'". */
export declare const formatToISO: (dateUTC: Date, outputDateFormat: string) => string;
/** Валидируем customViewDate (строка -> Date(UTC) или null). */
export declare const validateCustomViewDate: (date?: string) => Date | null;
export declare const hasDateLimit: (checkedDate: Date | null, minDate: Date | null, maxDate: Date | null) => boolean;
/** Сравниваем две даты (UTC) по году, месяцу, числу. */
export declare const isSameDates: (d1: Date, d2: Date) => boolean;
/** Сравниваем два месяца (UTC) (год и месяц). */
export declare const isSameMonths: (d1: Date, d2: Date) => boolean;
/** Проверяем валидность времени (val) в заданном диапазоне timeRange. Возвращаем true, если НЕвалидно. */
export declare const isTimeValid: (val: {
    hours: string;
    minutes: string;
}, timeRange?: [string, string]) => boolean;
/**
 * Возвращаем массив недель (каждая неделя – 7 чисел или null) для указанного локального года/месяца.
 * @param localYear Год
 * @param localMonth Месяц
 * @param showAdjacentMonthDays Объект с настройками отображения дней соседних месяцев
 */
export declare const getWeeksOfMonth: (localYear: number, localMonth: number, showAdjacentMonthDays?: {
    showPrevious?: boolean;
    showNext?: boolean;
}) => Array<Array<{
    day: number | null;
    month: number;
    year: number;
} | null>>;
/** Простой хелпер для генерации диапазона лет. */
export declare const getYearsRange: (startYear: number, endYear: number) => number[];
/**
 * Сдвиг месяца влево/вправо (delta=-1 или +1) без мутаций исходной даты.
 * Сохраняет день месяца, если он существует в целевом месяце, иначе клемпит до последнего дня.
 * Возвращает новую дату в UTC.
 */
export declare const shiftMonthUTC: (viewDateUTC: Date, timeZone: string, delta: number, enableTimePicker: boolean) => Date;
