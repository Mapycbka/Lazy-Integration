import { DropdownWithModeType } from ':components/NewDropdownMenu/NewDropdownMenu.types';
import type { Placement } from '@floating-ui/react';
import type { ReactNode, RefObject } from 'react';
import { TestComponentProps } from 'src/types/test-component';
export interface MenuProps extends TestComponentProps {
    parent: RefObject<HTMLElement>;
    children: ReactNode;
    isOpen: boolean;
    onOutsideClick?: (event: MouseEvent) => void;
    className?: string;
    /**
     * @deprecated
     * Этот пропс был служебным, для использования внутри компонента. Если вы по какой то причине его использовали - больше не используйте.
     * Для управления позицией меню слева/справа будет новый пропс.
     * Будет удалено в следующих релизах.
     */
    internalPosition?: 'left' | 'right';
    nestedPosition?: 'left' | 'right' | 'auto';
    align?: Placement;
    onOpen?: () => void;
    onClose?: () => void;
    onEscape?: () => void;
    id?: string;
    widthMode?: DropdownWithModeType;
}
export interface MenuContextType {
    focusedId: string;
    setFocusedId: (index: string) => void;
    addId: (id: string) => void;
    ids: string[];
}
export interface MenuProviderProps {
    children: ReactNode;
}
