import type { ButtonProps } from '../../Button';
export interface PaginationCellProps extends Omit<ButtonProps, 'variant'> {
    /** Тип кнопки, определяет стили */
    variant: 'text' | 'icon';
    /** Признак активная / неактивная кнопка, определяет цвет кнопки */
    active?: boolean;
    /** Пользовательский класс для корневого тега */
    className?: string;
}
