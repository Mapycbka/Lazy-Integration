import type { ReactNode } from 'react';
export type ProgressBarProps = BarChartProps | DonutChartProps;
export interface BarChartProps extends BaseProps {
    type: 'BarChart';
}
export interface DonutChartProps extends BaseProps {
    type: 'DonutChart';
    innerElement?: ReactNode;
    innerRadius?: number;
}
interface BaseProps {
    values: ProgressBarValue[];
    width?: number;
    height?: number;
    maxValue?: number;
    onMouseLeave?: (event: any, item: any, ref: any) => void;
    onMouseMove?: (event: any, item: any, ref: any) => void;
    className?: string;
}
export interface ProgressBarValue {
    value: number;
    label?: string;
    color: ProgressBarColors;
}
export declare enum ProgressBarColors {
    grey = "Grey",
    red = "Red",
    orange = "Orange",
    green = "Green",
    blue = "Blue",
    teal = "Teal",
    magenta = "Magenta",
    purple = "Purple",
    aquamarine = "Aquamarine",
    yellow = "Yellow",
    saphire = "Saphire"
}
export {};
