import { jsx } from 'react/jsx-runtime';

function Message({ children, type }) {
    return jsx("div", { className: `react-pdf__message react-pdf__message--${type}`, children: children });
}

export { Message as default };
