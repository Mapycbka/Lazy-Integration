import { jsx } from 'react/jsx-runtime';
import { classNames } from '../../../helpers/classnames.js';

const TableHead = ({ className, children, shadow = false, ...props }) => (jsx("thead", { "data-testid": "TableHead", ...props, className: classNames('dsb_table-head', shadow && 'dsb_table-head__shadow', className), children: children }));

export { TableHead };
