import type { ReactEventHandler, ReactNode } from 'react';
export interface RenderFn {
    isExpanded: boolean;
    onClick: (id: string | number) => ReactEventHandler;
    item: TableRowItem;
    depth: number;
    hasChildren: boolean;
    className?: string;
}
export interface TableRowsProps {
    items?: TableRowItem[];
    hidden?: boolean;
    depth?: number;
    className?: string;
    expandedOnOpen?: boolean;
    render: ({ isExpanded, onClick, item, depth, hasChildren }: RenderFn) => ReactNode;
}
export interface TableRowItem {
    id: string | number;
    children?: TableRowItem[];
    content?: Record<string, any>;
}
