import type { TimePickerProps } from '../TimePicker.types';
export interface RangeTimeValue {
    startTime: string;
    endTime: string;
}
export type TimePickerRangeSplitProps = Omit<TimePickerProps, 'onChange' | 'showIcon' | 'disabled' | 'helperText'> & {
    /**
     * Выключает время в начале
     */
    disableStartTime?: boolean;
    /**
     * Выключает время в конце
     */
    disableEndTime?: boolean;
    /**
     * Значение по умолчанию в начале
     */
    defaultValueStart?: string;
    /**
     * Значение по умолчанию в конце
     */
    defaultValueEnd?: string;
    onChange?: (time: RangeTimeValue) => void;
    /**
     * Подсветить таймпикер начало красной рамкой ошибки  - управляется с наружи
     */
    errorStart?: boolean;
    /**
     * Подсветить таймпикер конец красной рамкой ошибки  - управляется с наружи
     */
    errorEnd?: boolean;
};
