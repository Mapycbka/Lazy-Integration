import { jsxs, jsx } from 'react/jsx-runtime';
import { Icons } from '@beeline/design-tokens/js/iconfont/icons';
import { forwardRef } from 'react';
import { classNames } from '../../helpers/classnames.js';
import { Icon } from '../Icon/Icon.js';

const iconsSet = new Set(Object.values(Icons));
const Informer = forwardRef(({ semantic = 'brand', size = 'medium', content, children, className, dataTestId = 'Informer', ...htmlAttributes }, ref) => {
    const hasChildren = Boolean(children);
    const isDot = content === undefined;
    const isIcon = !isDot && typeof content === 'string' && iconsSet.has(content);
    return (jsxs("span", { ...htmlAttributes, "data-testid": dataTestId, className: classNames('dsb_informer', hasChildren ? 'dsb_informer--with-child' : 'dsb_informer--standalone', className), ref: ref, children: [children && jsx("span", { className: "dsb_informer__target", children: children }), jsxs("span", { className: classNames('dsb_informer__badge', `dsb_informer__badge--semantic-${semantic}`, `dsb_informer__badge--${size}`, !hasChildren && 'dsb_informer__badge--standalone', isDot && 'dsb_informer__badge--dot', isIcon && 'dsb_informer__badge--icon'), "data-testid": `${dataTestId}-badge`, children: [!isDot && !isIcon && content, isIcon && (jsx("span", { className: "dsb_informer__icon", children: jsx(Icon, { iconName: content }) }))] })] }));
});

export { Informer };
