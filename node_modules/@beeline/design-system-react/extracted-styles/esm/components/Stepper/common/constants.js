import { Icons } from '@beeline/design-tokens/js/iconfont/icons';

const ICON_STATE_MAP = {
    success: true,
    error: true,
    disabled: true,
};
const ORDER_STATE_MAP = {
    active: true,
    visited: true,
    'non-visited': true,
};
const STEP_ICON_MAP = {
    success: Icons.CheckCircled,
    error: Icons.InfoCircled,
    disabled: Icons.Lock,
};

export { ICON_STATE_MAP, ORDER_STATE_MAP, STEP_ICON_MAP };
