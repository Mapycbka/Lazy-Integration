import { jsx } from 'react/jsx-runtime';
import { classNames } from '../../../helpers/classnames.js';
import { Typography } from '../../Typography/Typography.js';

const TooltipContent = ({ title, className }) => (jsx("div", { "data-testid": "TooltipContent", className: classNames('dsb_tooltip', className), children: jsx(Typography, { variant: "caption", className: "dsb_tooltip__title", children: title }) }));

export { TooltipContent };
