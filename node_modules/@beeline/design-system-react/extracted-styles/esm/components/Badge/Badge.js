import { jsxs, jsx } from 'react/jsx-runtime';
import { classNames } from '../../helpers/classnames.js';
import { Icon } from '../Icon/Icon.js';

const Badge = ({ children, type = 'secondary', semantic = 'info', icon, dot = false, className, dataTestId = 'Badge', ...props }) => {
    const showIcon = !dot && icon;
    const semanticClassMap = {
        danger: 'error',
    };
    const semanticToken = semanticClassMap[semantic] ?? semantic;
    return (jsxs("span", { "data-testid": dataTestId, className: classNames('dsb_badge', `dsb_badge__${type}`, `dsb_badge__${semanticToken}`, {
            'dsb_badge__has-dot': dot,
            'dsb_badge__has-icon': !!showIcon,
        }, className), ...props, children: [dot && jsx("span", { className: "dsb_badge__dot", "aria-hidden": "true" }), showIcon && (jsx("span", { className: "dsb_badge__icon", "aria-hidden": "true", children: jsx(Icon, { iconName: icon, size: "small" }) })), jsx("span", { className: "dsb_badge__text", children: children })] }));
};

export { Badge };
