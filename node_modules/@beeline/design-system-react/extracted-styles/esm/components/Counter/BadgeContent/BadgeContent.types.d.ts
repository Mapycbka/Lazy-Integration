import type { CounterProps } from '../Counter.types';
export interface BadgeContentProps extends Pick<CounterProps, 'count' | 'warning' | 'error' | 'hideBadgeContent' | 'size'> {
    /**
     * флаг сообщающий что бейдж используется без родителя, а значит не нужно вырывать его из потока
     */
    stanalone: boolean;
}
