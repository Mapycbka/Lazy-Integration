import { jsxs, Fragment, jsx } from 'react/jsx-runtime';
import { Icon } from '../../Icon/Icon.js';
import { Menu } from '../../Menu/Menu.js';
import { MenuItem } from '../../Menu/MenuItem/MenuItem.js';
import { Typography } from '../../Typography/Typography.js';
import { classNames } from '../../../helpers/classnames.js';
import { useRef } from 'react';

const ToolbarItem = ({ label, icon, selected, disabled, onClick, size = 'medium', menu, }) => {
    const itemRef = useRef(null);
    return (jsxs(Fragment, { children: [jsxs("div", { ref: itemRef, className: classNames('dsb_toolbar__item', `dsb_toolbar__item-${size}`, disabled && 'dsb_toolbar__item-disabled', selected && 'dsb_toolbar__item-selected'), onClick: onClick, children: [icon && jsx(Icon, { color: icon.color, size: size, iconName: icon.iconName }), jsx(Typography, { variant: 'body3', children: label })] }), menu && (jsx(Menu, { onOutsideClick: menu.onOutsideClick, isOpen: menu.isOpen, parent: itemRef, children: menu.items.map((item, index) => (jsx(MenuItem, { iconName: item?.iconName, subtitle: item?.subtitle, disabled: item.disabled, onClick: item.onClick, title: item?.title }, index))) }))] }));
};

export { ToolbarItem };
