import { jsxs, jsx } from 'react/jsx-runtime';
import { Icon } from '../../Icon/Icon.js';
import { Typography } from '../../Typography/Typography.js';
import { classNames } from '../../../helpers/classnames.js';
import React__default, { isValidElement } from 'react';

const TabbarItem = ({ className, type, iconName, label, active, onClick, routingComponent, disabled, }) => {
    const WrapItem = item => isValidElement(routingComponent) ? React__default.cloneElement(routingComponent, {}, item) : item;
    return WrapItem(jsxs("li", { role: "button", tabIndex: onClick ? 0 : undefined, className: classNames('dsb_tabbarItem', `dsb_tabbarItem-${type}`, active && 'dsb_tabbarItem-active', disabled && 'dsb_tabbarItem-disabled', className), onClick: onClick, onKeyDown: onClick
            ? event => {
                if (event.key === 'Enter' || event.key === ' ') {
                    event.preventDefault();
                    // Создаем синтетическое событие мыши для передачи в onClick
                    const mouseEvent = new MouseEvent('click');
                    onClick(mouseEvent);
                }
            }
            : undefined, children: [jsx(Icon, { iconName: iconName, size: type === 'default' ? 'large' : 'small', className: "dsb_tabbarItem__icon" }), type === 'default' && (jsx(Typography, { variant: "caption", className: "dsb_tabbarItem__label", children: label }))] }));
};

export { TabbarItem };
