import type { MutableRefObject } from 'react';
import type { SVGPathSettings } from './ProgressButton.types';
/** Рассчёт размеров контейнера и радиуса границ для отрисовки в нём svg */
export declare const calcSettings: (buttonRef: MutableRefObject<HTMLButtonElement>) => SVGPathSettings;
export declare const calcPathDefinition: (settings: SVGPathSettings) => string;
/**
 * проверяет является ли переданный объект оьектом ошибки
 *
 * @param err потенциальный объект ошибки
 */
export declare const isErrorObject: (err: any) => boolean;
