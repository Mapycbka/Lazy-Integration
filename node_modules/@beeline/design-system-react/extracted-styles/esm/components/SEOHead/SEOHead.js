import { jsxs, jsx } from 'react/jsx-runtime';
import { Helmet } from '../../node_modules/react-helmet-async/lib/index.esm.js';
import { validateSEOProps } from './utils.js';

const SEOHead = ({ title, description, canonicalUrl, lang = 'ru', robots = 'index, follow', ogTitle, ogDescription, ogType = 'website', ogUrl, ogImage, twitterCard = 'summary_large_image', favicon = '/favicon.ico', appleTouchIcon, charset = 'UTF-8', viewport = 'width=device-width, initial-scale=1.0', children, dataTestId = 'SEOHead', }) => {
    if (process.env.NODE_ENV !== 'production') {
        validateSEOProps({ title, description });
    }
    const robotsContent = Array.isArray(robots) ? robots.join(', ') : robots;
    const finalOgTitle = ogTitle || title;
    const finalOgDescription = ogDescription || description;
    const finalOgUrl = ogUrl || canonicalUrl;
    return (jsxs(Helmet, { "data-testid": dataTestId, children: [jsx("html", { lang: lang }), jsx("meta", { charSet: charset }), jsx("meta", { name: "viewport", content: viewport }), jsx("title", { children: title }), jsx("meta", { name: "description", content: description }), jsx("meta", { name: "robots", content: robotsContent }), jsx("link", { rel: "canonical", href: canonicalUrl }), jsx("meta", { property: "og:title", content: finalOgTitle }), jsx("meta", { property: "og:description", content: finalOgDescription }), jsx("meta", { property: "og:type", content: ogType }), jsx("meta", { property: "og:url", content: finalOgUrl }), ogImage && jsx("meta", { property: "og:image", content: ogImage }), jsx("meta", { name: "twitter:card", content: twitterCard }), jsx("link", { rel: "icon", href: favicon }), appleTouchIcon && jsx("link", { rel: "apple-touch-icon", href: appleTouchIcon }), children] }));
};
SEOHead.displayName = 'SEOHead';

export { SEOHead };
