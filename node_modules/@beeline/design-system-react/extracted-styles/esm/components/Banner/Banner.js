import { jsxs, jsx } from 'react/jsx-runtime';
import '../../helpers/popupsContext.js';
import { useDeviceSize } from '../../helpers/use-device-size.js';
import { classNames } from '../../helpers/classnames.js';
import { Icons } from '@beeline/design-tokens/js/iconfont/icons';
import { forwardRef } from 'react';
import { Icon } from '../Icon/Icon.js';
import { IconButton } from '../IconButton/IconButton.js';
import { ButtonContainer } from './ButtonContainer/ButtonContainer.js';

const Banner = forwardRef(({ title, type = 'horizontal', color = 'info', actions, onClose, iconName, className }, ref) => {
    const { isExtraSmallDevice } = useDeviceSize();
    const _type = isExtraSmallDevice ? 'vertical' : type;
    if (type === 'horizontal' && isExtraSmallDevice) {
        console.warn('[dsb-banner] Для мобильного баннер доступен только type = vertical');
    }
    return (jsxs("div", { role: "banner", "data-testid": "Banner", ref: ref, className: classNames('banner banner-container', `banner__type-${_type}`, `banner__color-${color}`, !!onClose && 'banner__close-button', className), children: [jsx("div", { className: "banner-title", children: jsxs("div", { className: "banner-title-text", children: [iconName && jsx(Icon, { size: "medium", iconName: iconName, className: "banner-title-icon" }), title] }) }), actions && jsx(ButtonContainer, { actions: actions, className: "banner-actions" }), onClose && (jsx(IconButton, { size: "large", className: "icon-button-close icon-close", onClick: onClose, iconName: Icons.Close, "aria-label": "\u0417\u0430\u043A\u0440\u044B\u0442\u044C \u0431\u0430\u043D\u043D\u0435\u0440" }))] }));
});

export { Banner };
