import { jsxs, Fragment, jsx } from 'react/jsx-runtime';
import { Typography } from '../../Typography/Typography.js';

const FileUploaderTitle = ({ title, subTitle, dragOver, linkName, dragOverTitle, }) => {
    const localTitleVariant = dragOver ? dragOverTitle : title;
    const isComponent = typeof localTitleVariant !== 'string';
    const localTitle = isComponent ? (localTitleVariant) : (jsxs(Fragment, { children: [jsxs("div", { className: "dsb_file-uploader-title_wrapper", children: [jsx(Typography, { variant: "body3", className: "dsb_file-uploader-title_text", children: localTitleVariant }), ' ', linkName && !dragOver && jsx("span", { className: "dsb_file-uploader-title_link", children: linkName })] }), subTitle && !dragOver && (jsx(Typography, { variant: "body3", className: "dsb_file-uploader-title_subtitle", children: subTitle }))] }));
    return (jsx("div", { "data-testid": "FileUploaderTitle", className: "dsb_file-uploader-title", children: localTitle }));
};

export { FileUploaderTitle };
