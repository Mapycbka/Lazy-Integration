import { jsx } from 'react/jsx-runtime';
import { classNames } from '../../../helpers/classnames.js';

const ExpansionPanelContent = ({ className, children, ...props }) => (jsx("div", { "data-testid": "ExpansionPanelContent", ...props, className: classNames('dsb_expansion-panel-content', className), children: children }));

export { ExpansionPanelContent };
