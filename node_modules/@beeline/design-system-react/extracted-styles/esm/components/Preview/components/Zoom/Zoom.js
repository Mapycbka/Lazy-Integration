import { jsxs, jsx } from 'react/jsx-runtime';
import { IconButton } from '../../../IconButton/IconButton.js';
import { Icons } from '@beeline/design-tokens/js/iconfont/icons';

const Zoom = ({ onZoomIn, onZoomOut, onZoomReset }) => (jsxs("div", { className: "dsb_preview__zoom", children: [jsx(IconButton, { iconName: Icons.Add, variant: "base", onClick: onZoomIn }), jsx(IconButton, { iconName: Icons.ZoomIn, variant: "base", onClick: onZoomReset }), jsx(IconButton, { iconName: Icons.Remove, variant: "base", onClick: onZoomOut })] }));

export { Zoom };
