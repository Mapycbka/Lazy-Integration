import { jsx } from 'react/jsx-runtime';
import { IconButton } from '../../../IconButton/IconButton.js';
import { Icons } from '@beeline/design-tokens/js/iconfont/icons';

const Fullscreen = ({ onClick }) => (jsx("div", { className: "dsb_preview__fullscreen", children: jsx(IconButton, { onClick: onClick, variant: "base", iconName: Icons.Expand }) }));

export { Fullscreen };
