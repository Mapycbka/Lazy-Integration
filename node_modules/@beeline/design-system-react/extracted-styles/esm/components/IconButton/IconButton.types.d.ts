import type { Icons } from '@beeline/design-tokens/js/iconfont/icons';
import type { TestComponentProps } from '../../types/test-component';
import type { ButtonProps } from '../Button';
type PickedButtonProps = Omit<ButtonProps, 'startIcon' | 'variant' | 'endIcon'>;
export type IconButtonVariant = 'base' | 'outlined' | 'plain' | 'overlay';
export interface IconButtonProps extends PickedButtonProps, TestComponentProps {
    /**
     * Название иконки: на основе Дизайн Токенов
     */
    iconName: Icons;
    /**
     * Текстовое описание кнопки для скринридеров (aria-label)
     */
    'aria-label'?: string;
    'aria-hidden'?: boolean;
    variant?: IconButtonVariant;
}
export {};
