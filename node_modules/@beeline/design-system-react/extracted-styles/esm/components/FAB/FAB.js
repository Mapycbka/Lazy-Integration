import { jsx } from 'react/jsx-runtime';
import { forwardRef } from 'react';
import { classNames } from '../../helpers/classnames.js';
import { Button } from '../Button/Button.js';
import '../Button/ProgressButton/ProgressButton.js';
import { Icon } from '../Icon/Icon.js';

const FAB = forwardRef(({ type = 'standard', iconName, className, children, dataTestId = 'FAB', ...props }, ref) => (jsx(Button, { "data-testid": dataTestId, ...props, ref: ref, startIcon: jsx(Icon, { iconName: iconName, size: "large" }), className: classNames('inverseTheme dsb_fab-button', `dsb_fab-button__${type}`, className), children: type === 'extended' && children })));

export { FAB };
