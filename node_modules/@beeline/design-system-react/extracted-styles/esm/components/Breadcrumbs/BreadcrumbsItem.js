import { jsxs, jsx, Fragment } from 'react/jsx-runtime';
import { Icon } from '../Icon/Icon.js';
import { Menu } from '../Menu/Menu.js';
import { MenuItem } from '../Menu/MenuItem/MenuItem.js';
import { Tooltip } from '../Tooltip/Tooltip.js';
import { Typography } from '../Typography/Typography.js';
import { classNames } from '../../helpers/classnames.js';
import { Icons } from '@beeline/design-tokens/js/iconfont/icons';
import { useRef } from 'react';

const BreadcrumbsItem = ({ label, href, routingComponent, tooltipText, currentPage, menu, index, }) => {
    const itemRef = useRef(null);
    const base = (jsxs(Typography, { ref: itemRef, itemProp: "name", linkState: currentPage ? undefined : 'enabled', className: classNames('dsb-breadcrumbs__item-builtin', currentPage && 'dsb-breadcrumbs__item-builtin-current'), variant: routingComponent ? 'routedLink' : 'nativeLink', href: routingComponent ? undefined : href, routingComponent: routingComponent, onClick: () => {
            if (menu) {
                menu.onItemClick();
            }
        }, children: [label, " ", menu && jsx(Icon, { iconName: Icons.ArrowDropDown })] }));
    return (jsxs(Fragment, { children: [tooltipText ? jsx(Tooltip, { title: tooltipText, children: base }) : base, menu && (jsx(Menu, { onOutsideClick: () => {
                    menu.onOutsideClick();
                }, isOpen: menu.isOpen, parent: itemRef, children: menu.items.map((item, index) => (jsx(MenuItem, { ...item }, index))) }))] }));
};

export { BreadcrumbsItem };
