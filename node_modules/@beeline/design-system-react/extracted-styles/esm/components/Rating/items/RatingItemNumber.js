import { jsx } from 'react/jsx-runtime';
import { Typography } from '../../Typography/Typography.js';
import { classNames } from '../../../helpers/classnames.js';

const RatingItemNumber = ({ selected, value }) => (jsx("div", { className: classNames('dsb_rating__select__item-number', selected && 'dsb_rating__select__item-number-selected'), children: jsx(Typography, { variant: "body3", children: value }) }));

export { RatingItemNumber };
