import { jsx } from 'react/jsx-runtime';
import { useMemo } from 'react';
import { classNames } from '../../../helpers/classnames.js';

const LinearProgress = ({ value = 0, type = 'solo', cycled = false, className, style, ...props }) => {
    const limitedValue = useMemo(() => Math.min(value, 100), [value]);
    const progressBarTransform = useMemo(() => `${limitedValue}%`, [limitedValue]);
    return (jsx("span", { "data-testid": "LinearProgress", ...props, style: { ...style }, className: classNames('dsb_progress', 'dsb_progress__linear', `dsb_progress__linear-${type}`, className), role: "progressbar", "aria-valuenow": value, "aria-valuemax": 100, "aria-valuemin": 0, children: jsx("span", { style: { width: progressBarTransform }, className: classNames('dsb_progress-bar', cycled && 'dsb_progress-bar__cycled', `dsb_progress-bar__${type}`) }) }));
};

export { LinearProgress };
