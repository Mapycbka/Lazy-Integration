/**
 * Предикаты для явной и читабельной ветвизации логики.
 */
export declare const isValidDate: (d?: Date | null) => d is Date;
export declare const hasStartedRange: (start?: Date | null) => boolean;
export declare const hasFilledRange: (end?: Date | null) => boolean;
/**
 * Строит локальную дату из координат клика в календаре и опциональных часов/минут.
 */
export declare const buildLocalFromClick: (day: number, month: number, year: number, hours?: number, minutes?: number) => Date;
/**
 * Конвертирует локальную дату в представление UTC с учётом режима таймпикера.
 */
export declare const toUTCFromLocal: (localDate: Date, enableTimePicker: boolean, effectiveTimeZone: string) => Date;
/**
 * Форматирует дату UTC в выходную строку по заданному формату.
 */
export declare const toOutputString: (dateUTC: Date, outputDateFormat: string) => string;
/**
 * Сравнивает только календарный день (год, месяц, число) между двумя датами UTC.
 */
export declare const isSameCalendarDayUTC: (aUTC: Date, bUTC: Date) => boolean;
/**
 * Вычисляет следующий диапазон при клике по дню.
 * Возвращает кортеж строк [start, end]. Пустой конец представлен как ''.
 */
export declare const computeNextRangeOnDayClick: (args: {
    startUTC?: Date | null;
    endUTC?: Date | null;
    localDate: Date;
    newUTC: Date;
    enableTimePicker: boolean;
    disabledStartDate: boolean;
    disabledEndDate: boolean;
    outputDateFormat: string;
    originalStartStr?: string;
    pickerStartTime?: {
        hours: string;
        minutes: string;
    };
    effectiveTimeZone?: string;
}) => [string, string];
export type RangeSelectionState = {
    startUTC?: Date | null;
    endUTC?: Date | null;
};
export type RangeSelectionAction = {
    type: 'dayClick';
    payload: {
        localDate: Date;
        newUTC: Date;
        enableTimePicker: boolean;
        disabledStartDate: boolean;
        disabledEndDate: boolean;
        outputDateFormat: string;
        originalStartStr?: string;
        pickerStartTime?: {
            hours: string;
            minutes: string;
        };
        effectiveTimeZone?: string;
    };
};
/**
 * Минимальный редьюсер выбора, делающий логику диапазона неизменяемой и явной.
 */
export declare const selectionReducer: (state: RangeSelectionState, action: RangeSelectionAction) => [string, string];
/**
 * Обновляет время диапазона для границы (старт/конец) в режиме времени.
 * Возвращает новые ISO-строки и обновлённый viewDate при необходимости.
 */
export declare const updateRangeTimeOnPick: (args: {
    val: {
        hours: string;
        minutes: string;
    };
    index: 0 | 1;
    value: [string, string];
    timeRange: [string, string];
    enableTimePicker: boolean;
    effectiveTimeZone: string;
}) => {
    invalid: boolean;
    newValue?: [string, string];
    newViewDateUTC?: Date;
};
