import { jsx, jsxs } from 'react/jsx-runtime';
import { Button } from '../../../../Button/Button.js';
import '../../../../Button/ProgressButton/ProgressButton.js';
import { Typography } from '../../../../Typography/Typography.js';
import { classNames } from '../../../../../helpers/classnames.js';

const formatDate = (date) => {
    if (!date) {
        return '';
    }
    if (typeof date === 'string') {
        // Если вдруг уже формат ДД.ММ.ГГГГ, не трогаем
        if (/^(?:\d{2}\.){2}\d{4}$/.test(date)) {
            return date;
        }
        // Иначе парсим как дату
        date = new Date(date);
    }
    const day = String(date.getDate()).padStart(2, '0');
    const month = String(date.getMonth() + 1).padStart(2, '0');
    const year = date.getFullYear();
    return `${day}.${month}.${year}`;
};
const PresetPeriods = ({ periods, onSelect, className, dataTestId = 'PresetPeriods', selectedRange, }) => (jsx("div", { className: classNames('dsb_preset-periods', className), "data-testid": dataTestId, children: periods.map((period, index) => (jsx("div", { style: { marginBottom: 0, width: 'calc(50% - 12px)' }, children: jsx(Button, { fullWidth: true, variant: "outlined", size: "small", className: "dsb_preset-periods-item", "data-testid": `${dataTestId}-item-${index}`, onClick: () => onSelect(period.range), children: jsxs("div", { children: [jsx(Typography, { variant: "body2", children: period.label }), jsxs(Typography, { variant: "body3", className: "dsb_preset-periods-date", children: [formatDate(period.range[0]), "\u2013", formatDate(period.range[1])] })] }) }) }, period.label))) }));

export { PresetPeriods };
