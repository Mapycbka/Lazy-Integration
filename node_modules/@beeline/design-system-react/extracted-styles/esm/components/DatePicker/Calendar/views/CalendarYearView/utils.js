const getYearsRange = (startYear, endYear) => startYear > endYear
    ? [startYear]
    : Array.from({ length: endYear - startYear + 1 })
        .fill(null)
        .map((empty, idx) => startYear + idx)
        .reverse();

export { getYearsRange };
