import type { Option } from '../../Select.types';
interface AddArrayIndex<T> {
    options?: T[];
    values?: T[];
}
export declare const useAddArrayIndex: <T>({ options, values }: AddArrayIndex<T>) => Option<T>[][];
export {};
