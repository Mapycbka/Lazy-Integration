import './DropDownContent.scss';
import React from 'react';
import type { FilterProps } from '../Filter';
import type { OptionsProps } from '../Options';
interface DropDownContent<T> extends OptionsProps<T>, FilterProps {
    selectRef: React.MutableRefObject<HTMLDivElement>;
    unstyled?: boolean;
}
export declare const DropDownContent: <T>({ selectedItems, optionsData, selectAll, filter, filterValue, filterFunction, selectAllIndex, optionsClassName, multiple, itemClassName, onMultiselectItem, onManualClickOption, onSelectItem, focusedIndex, handleSelectAll, makeOption, showTooltip, defaultMakeOption, selectAllText, loading, loadingText, loadingContent, noOptionsText, optionsRef, selectStatus, filterPlaceholder, filterRef, changeFilterValue, dataTestId, selectRef, makeOptionWrapper, groups, unstyled, }: DropDownContent<T>) => React.JSX.Element;
export {};
