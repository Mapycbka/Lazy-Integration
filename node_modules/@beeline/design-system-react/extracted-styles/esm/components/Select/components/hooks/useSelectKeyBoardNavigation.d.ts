import type { RefObject } from 'react';
import type { Option } from '../../Select.types';
interface SelectKeyBoardNavigation<T> {
    enableKeyboardNavigation?: boolean;
    isOpen?: boolean;
    options?: Array<Option<T>>;
    handleClose?: () => void;
    multiple?: boolean;
    optionsRef?: React.MutableRefObject<HTMLDivElement>;
    inputRef?: RefObject<HTMLInputElement>;
    /** Ref на контейнер dropdown (filter + options). Используется для проверки фокуса внутри выпадашки. */
    dropdownRef?: RefObject<HTMLDivElement | null>;
    onMultiselectItem?: (item: Option<T>) => () => void;
    onSelectItem: (item: Option<T>) => () => void;
    onManualClickOption?: (option: T) => void;
    selectAll: boolean;
    filter?: boolean;
    filterValue?: string;
    filterFunction?: (options: Array<Option<T>>, filterValue: string) => Array<Option<T>>;
    groups?: Array<Option<string>>;
}
export declare const useSelectKeyBoardNavigation: <T>({ enableKeyboardNavigation, isOpen, options, handleClose, multiple, optionsRef, onMultiselectItem, onSelectItem, onManualClickOption, selectAll, inputRef, dropdownRef, filter, filterValue, filterFunction, groups, }: SelectKeyBoardNavigation<T>) => readonly [number, import("react").Dispatch<import("react").SetStateAction<number>>];
export {};
