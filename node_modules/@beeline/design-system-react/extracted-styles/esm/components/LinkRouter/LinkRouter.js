import { jsx } from 'react/jsx-runtime';
import { classNames } from '../../helpers/classnames.js';
import { Typography } from '../Typography/Typography.js';

/**
 * @deprecated LinkRouter никогда не был готовым к использованию компонентом,
 * но если вы по какой то причине его использовали - больше не используйте.
 * Компонент ссылок реализован как части компонента Typography
 */
const LinkRouter = ({ children, href, isActive = false, className }) => (jsx("a", { "data-testid": "LinkRouter", className: "dsb-link-router", href: href, children: jsx(Typography, { variant: 'body3', className: classNames(className, 'dsb-link-router__text', isActive ? 'active-link' : 'inactive-link'), children: children }) }));

export { LinkRouter };
