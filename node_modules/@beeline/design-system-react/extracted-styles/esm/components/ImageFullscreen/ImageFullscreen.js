import { jsxs, jsx } from 'react/jsx-runtime';
import React__default from 'react';
import { Typography } from '../Typography/Typography.js';
import { classNames } from '../../helpers/classnames.js';

const ImageFullscreen = ({ header, headline, image, withBackground = true, className, dataTestId = 'ImageFullscreen', ...rest }) => {
    const base = 'dsb_imagefullscreen';
    return (jsxs("section", { className: classNames(base, withBackground && `${base}-background`, className), "data-testid": dataTestId, ...rest, children: [header && (jsx(Typography, { variant: 'h2', className: `${base}__header`, children: header })), headline && typeof headline === 'string' ? (jsx(Typography, { inactive: true, variant: "body2", className: `${base}__headline`, children: headline })) : (headline), jsx("div", { className: `${base}__image`, children: typeof image === 'object' && !React__default.isValidElement(image) ? jsx("img", { loading: "lazy", ...image }) : image })] }));
};

export { ImageFullscreen, ImageFullscreen as default };
