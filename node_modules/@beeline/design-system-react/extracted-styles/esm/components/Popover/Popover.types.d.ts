import type { Placement } from '@floating-ui/react';
import type { ReactNode, RefObject } from 'react';
import type { TestComponentProps } from '../../types/test-component';
export interface PopoverProps extends TestComponentProps {
    /**
     * Сотояние отображения: отображено / скрыто.
     */
    open?: boolean;
    /**
     * Отключает отображение при событии наведения.
     */
    disableHover?: boolean;
    /**
     * Режим отображения: по наведению, по клику или оба.
     * @default 'hover'
     */
    triggerMode?: 'hover' | 'click' | 'both';
    /**
     * Режим закрытия поповера
     * - 'auto': закрывается при клике вне поповера или по триггеру (по умолчанию)
     * - 'manual': закрывается только программно через open={false}
     * - 'content-click': закрывается при клике по содержимому поповера
     * - 'outside-click': закрывается только при клике вне поповера
     * @default 'auto'
     */
    closeMode?: 'auto' | 'manual' | 'content-click' | 'outside-click';
    /**
     * Автоматически закрывать поповер при скролле страницы
     * @default false
     */
    closeOnScroll?: boolean;
    /**
     * Callback, вызываемый при скролле страницы (когда поповер открыт)
     * Если возвращает true, поповер будет закрыт
     */
    onScroll?: (event: Event) => boolean | void;
    /**
     * Компонент, отображаемый внутри компонента.
     */
    content: ReactNode;
    children: PopoverChildren;
    /**
     * Пользовательский класс для тела компонента popover.
     */
    popoverClassName?: string;
    /**
     * Делает компонент popover не интерактивным.
     */
    noInteract?: boolean;
    /**
     * Пользовательский класс для добавления класса порталу.
     */
    portalClassName?: string;
    /**
     * Прикреплять портал к children вместо root
     * @default false
     */
    attachToChildren?: boolean;
    'aria-describedby'?: string;
    /**
     * Положение поповера относительно якорного элемента (children)
     * Совместимо с floating-ui: 'top', 'bottom', 'left', 'right' и варианты '-start'/'-end'
     * @default 'bottom'
     */
    placement?: Placement;
    /**
     * Длительность анимации появления/скрытия (в мс).
     * Может быть числом (для обоих) или объектом { enter, exit }.
     * @default 0
     */
    transitionDuration?: number | {
        enter: number;
        exit: number;
    };
}
export interface PopoverChildren extends React.JSX.Element {
    ref?: RefObject<HTMLElement>;
}
export interface PopoverPortalProps extends TestComponentProps {
    className?: string;
    content: ReactNode;
    wrappedContentRect: DOMRect;
    noInteract?: boolean;
    /**
     * Пользовательский класс для добавления класса порталу.
     */
    portalClassName?: string;
    /**
     * Прикреплять портал к children вместо root
     * @default false
     */
    attachToChildren?: boolean;
    /**
     * Ссылка на элемент-триггер (для позиционирования)
     */
    targetRef?: RefObject<HTMLElement>;
    'aria-describedby'?: string;
    /**
     * Положение поповера
     */
    placement?: Placement;
    /**
     * Стили для анимации
     */
    style?: React.CSSProperties;
}
