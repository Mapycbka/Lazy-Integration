import { jsxs, jsx } from 'react/jsx-runtime';
import { Icon } from '../../Icon/Icon.js';
import { classNames } from '../../../helpers/classnames.js';
import { Icons } from '@beeline/design-tokens/js/iconfont/icons';
import { Slides } from './Slides/Slides.js';
import { Slide } from './Slides/Slide/Slide.js';

const PopoverOnboarding = ({ open, activeSlideIndex = 0, onClose, className, onPaginationChange, content, ...props }) => {
    const isSlides = Array.isArray(content);
    return (jsxs("div", { ...props, className: classNames('dsb_onboarding inverseTheme', open && 'dsb_onboarding__open', className), "data-testid": "PopoverOnboarding", children: [onClose && jsx(Icon, { iconName: Icons.Close, size: "large", onClick: onClose, className: "dsb_onboarding-icon-close" }), isSlides && (jsx(Slides, { content: content, activeSlideIndex: activeSlideIndex, onPaginationChange: onPaginationChange })), !isSlides && jsx(Slide, { ...content })] }));
};

export { PopoverOnboarding };
