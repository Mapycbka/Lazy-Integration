import type { PopoverOnboardingProps } from '../PopoverOnboarding.types';
import type { SlideProps } from './Slide/Slide.types';
interface PickOnboardingType extends Pick<PopoverOnboardingProps, 'activeSlideIndex' | 'onPaginationChange'> {
}
export interface SlidesProps extends PickOnboardingType {
    content: SlideProps[];
}
export {};
