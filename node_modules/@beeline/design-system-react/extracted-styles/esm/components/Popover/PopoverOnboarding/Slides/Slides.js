import { jsxs, Fragment, jsx } from 'react/jsx-runtime';
import { Slide } from './Slide/Slide.js';
import { Stepper } from './Stepper/Stepper.js';

const Slides = ({ content, activeSlideIndex = 0, onPaginationChange }) => (jsxs(Fragment, { children: [jsx(Slide, { ...content[activeSlideIndex] }), content.length && (jsx(Stepper, { content: content, activeSlideIndex: activeSlideIndex, onPaginationChange: onPaginationChange }))] }));

export { Slides };
