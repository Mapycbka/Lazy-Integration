import type { PopoverOnboardingProps } from '../../PopoverOnboarding.types';
import type { SlideProps } from '../Slide/Slide.types';
export interface StepperProps extends Pick<PopoverOnboardingProps, 'onPaginationChange' | 'activeSlideIndex'> {
    content: SlideProps[];
}
