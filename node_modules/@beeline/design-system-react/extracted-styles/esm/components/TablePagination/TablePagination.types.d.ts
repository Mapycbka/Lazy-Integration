import type React from 'react';
export interface TablePaginationProps extends React.HTMLAttributes<HTMLDivElement> {
    /** Основной текст */
    label?: React.ReactNode;
    /** Количество строк таблицы*/
    rowsCount: number;
    /** Массив из параметров, определяющих количество строк на странице при пагинации */
    rowsPerPageOptions?: number[];
    /** Количество строк на странице при пагинации */
    rowsPerPage?: number;
    /** Текущая страница*/
    page: number;
    /**
     * Событие изменения текущей страницы
     *
     * @deprecated используйте {@link onUserActions}
     */
    onPageChange?: (page: number) => void;
    /**
     * событие изменения пользователем текущей страницы или количества строк на странице
     *
     * Не рекомендуется использовать вместе с {@link onPageChange} и {@link onRowsPerPageChange}
     *
     * @param event данные о текущей странице и кол-ве строк на странице
     */
    onUserActions?: (event: PaginationChangeEvent) => void;
    /**
     * Событие изменения параметра количества строк на странице
     *
     * @deprecated используйте {@link onUserActions}
     */
    onRowsPerPageChange?: (value: number) => void;
    /** Признак отображать кнопки перехода к первой/ последней страницам */
    showFirstAndLastButtons?: boolean;
}
export interface PaginationChangeEvent extends Required<Pick<TablePaginationProps, 'rowsPerPage' | 'page'>> {
}
