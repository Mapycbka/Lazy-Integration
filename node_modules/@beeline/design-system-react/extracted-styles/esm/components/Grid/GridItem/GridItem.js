import { jsx } from 'react/jsx-runtime';
import { Box } from '../../Box/Box.js';
import { classNames } from '../../../helpers/classnames.js';

const GridItem = ({ colSpan, rowSpan, colStart, rowStart, align, justify, className, dataTestId = 'GridItem', ...rest }) => {
    const gridItemClassName = classNames('dsb_grid-item', colSpan && `dsb_grid-item__col-span-${colSpan}`, rowSpan && `dsb_grid-item__row-span-${rowSpan}`, colStart && `dsb_grid-item__col-start-${colStart}`, rowStart && `dsb_grid-item__row-start-${rowStart}`, align && `dsb_grid-item__align-${align}`, justify && `dsb_grid-item__justify-${justify}`, className);
    return jsx(Box, { className: gridItemClassName, dataTestId: dataTestId, ...rest });
};
GridItem.displayName = 'GridItem';

export { GridItem };
