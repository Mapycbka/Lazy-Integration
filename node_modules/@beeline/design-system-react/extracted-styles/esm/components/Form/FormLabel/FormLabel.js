import { jsx } from 'react/jsx-runtime';
import { Typography } from '../../Typography/Typography.js';
import { classNames } from '../../../helpers/classnames.js';

const FormLabel = ({ required = false, htmlFor, className, children, dataTestId = 'FormLabel', ...props }) => {
    const Tag = htmlFor ? 'label' : 'span';
    return (jsx(Tag, { htmlFor: htmlFor, className: classNames('dsb_form-label', { 'dsb_form-label__required': required }, className), "data-testid": dataTestId, ...props, children: jsx(Typography, { variant: "body3", className: "dsb_form-label-text", children: children }) }));
};
FormLabel.displayName = 'FormLabel';

export { FormLabel };
