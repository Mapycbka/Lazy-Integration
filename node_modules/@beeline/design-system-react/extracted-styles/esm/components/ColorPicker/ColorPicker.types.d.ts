export interface IColorItem {
    hex: string;
    title: string;
}
export interface ColorPickerProps {
    color?: string | null;
    clearable?: boolean;
    onChange?: (color: string | null) => void;
}
