import { jsxs, jsx } from 'react/jsx-runtime';
import { classNames } from '../../../../helpers/classnames.js';
import { Typography } from '../../../Typography/Typography.js';
import { TimelineAtom } from '../../TimelineAtom/TimelineAtom.js';

const VerticalContent = ({ id, type, position, title, text, signature, action, collapsable = false, collapsed = false, iconName, }) => {
    const variant = type === 'active' || type === 'inactive' ? 'body2' : 'subtitle2';
    return (jsxs("li", { "data-testid": "VerticalContent", className: "dsb-timeline__vertical", role: "listitem", children: [jsx(TimelineAtom, { type: type, position: position, direction: "vertical", collapsable: collapsable, collapsed: collapsed, iconName: iconName }), jsxs("div", { className: "dsb-timeline-body", children: [jsxs("div", { className: classNames('dsb-timeline-body-title', `dsb-timeline-body-title__${type}`), children: [title && (jsx(Typography, { variant: variant, className: classNames('dsb-timeline-body-title-name', `dsb-timeline-body-title-name__${type}`), children: title })), signature && (jsx(Typography, { variant: "caption", className: classNames('dsb-timeline-body-title-signature', `dsb-timeline-body-title-signature__${type}`), children: signature }))] }), text && (jsx(Typography, { variant: "body3", className: classNames('dsb-timeline-body-text', `dsb-timeline-body-text__${type}`), children: text })), (type === 'current' || type === 'error') && jsx("div", { className: "dsb-timeline-body-action", children: action })] })] }, id));
};

export { VerticalContent };
