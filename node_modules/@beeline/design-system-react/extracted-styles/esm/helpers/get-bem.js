const getBEMClasses = (blockName) => [
    blockName,
    (modifierName) => `${blockName}--${modifierName}`,
    (elementName) => getBEMClasses(`${blockName}__${elementName}`),
];

export { getBEMClasses };
