import type { MutableRefObject } from 'react';
/**
 * подписывается на изменение размера элемента из переданного рефа. Работает с небольшой задержкой для избежания множественных срабатываний
 *
 * @param targetRef реф на целевой элемент
 *
 * @returns актуальная ширина элемента, переданного на вход
 */
export declare const useDropdownResizer: (targetRef: MutableRefObject<HTMLElement>) => {
    dropdownWidth: number;
};
