import { jsx } from 'react/jsx-runtime';
import { classNames } from '../../../helpers/classnames.js';
import { FileUploaderListItem } from './FileUploaderListItem/FileUploaderListItem.js';

const FileUploaderList = ({ files, onRemove, onRefresh, className, ...props }) => {
    if (!files?.length) {
        return null;
    }
    return (jsx("div", { "data-testid": "FileUploaderList", className: classNames('dsb_file-uploader-file-list', className), role: "list", ...props, children: files.map((file, index) => {
            const { name, state = 'upload', type, ...rest } = file;
            return (jsx(FileUploaderListItem, { name: name, state: state, onRefresh: onRefresh, onRemove: onRemove, type: type, ...rest }, index));
        }) }));
};

export { FileUploaderList };
//# sourceMappingURL=FileUploaderList.js.map
