import { jsxs, Fragment, jsx } from 'react/jsx-runtime';
import { Icons } from '@beeline/design-tokens/js/iconfont/icons';
import React__default from 'react';
import { classNames } from '../../../../helpers/classnames.js';
import { Icon } from '../../../Icon/Icon.js';
import { Progress } from '../../../Progress/Progress.js';
import { Typography } from '../../../Typography/Typography.js';
import './FileUploaderListItem2.js';
import { mapFileIcon } from './helper.js';

const FileUploaderListItem = ({ name, state = 'upload', onRefresh, onRemove, iconName, id, type, className, helperText, percent, size, loaded, actions, ...props }) => {
    const handleRefresh = () => {
        onRefresh?.({ id, name, state, iconName, type });
    };
    const handleRemove = () => {
        onRemove?.({ id, name, state, iconName, type });
    };
    const fileIcon = mapFileIcon(type);
    const actionsViews = (actions, state) => {
        if (React__default.isValidElement(actions)) {
            return actions;
        }
        return actions.map((fileAction) => (!fileAction.showStatuses || fileAction.showStatuses.includes(state)) && (jsx("span", { className: "dsb_file-uploader-file-delete", children: jsx(Icon, { iconName: fileAction.icon, size: "large", onClick: () => fileAction.onClick({
                    id,
                    name,
                    state,
                    iconName,
                    type,
                }) }) })));
    };
    const defaultActions = actions ? (actionsViews(actions, state)) : (jsxs(Fragment, { children: [state === 'error' && (jsx("span", { className: "dsb_file-uploader-file-refresh", children: jsx(Icon, { iconName: Icons.Refresh, size: "large", onClick: handleRefresh }) })), jsx("span", { className: "dsb_file-uploader-file-delete", children: jsx(Icon, { iconName: Icons.Close, size: "large", onClick: handleRemove }) })] }));
    return (jsxs("div", { className: "dsb_file-uploader_file", role: "listitem", children: [jsxs("div", { "data-testid": "FileUploaderListItem", className: classNames('dsb_file-uploader-file-description', className), ...props, children: [jsx("div", { className: "dsb_file-uploader-file_name-wrapper", children: jsxs("span", { className: "dsb_file-uploader-file_name", children: [jsx("span", { className: "dsb_file-uploader-file-icon", children: fileIcon }), jsxs(Typography, { variant: "body2", className: "dsb_file-uploader-file_name-text", children: [name, helperText && jsx("span", { className: classNames(state === 'error' && 'error-text'), children: helperText })] })] }) }), defaultActions] }), jsxs("div", { className: "dsb_file-uploader-file-progress", children: [state === 'process' && (jsx(Progress, { className: "progress-style", cycled: !!percent, value: percent ? percent : 0 })), percent && (jsxs("div", { className: "size-line", children: [jsxs("span", { children: [size, " \u0438\u0437 ", loaded] }), jsxs("span", { className: "percent", children: [percent, "%"] })] }))] })] }));
};

export { FileUploaderListItem };
//# sourceMappingURL=FileUploaderListItem.js.map
