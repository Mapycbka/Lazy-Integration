import { jsx, Fragment, jsxs } from 'react/jsx-runtime';
import './ExpansionPanelTitle2.js';
import { Icons } from '@beeline/design-tokens/js/iconfont/icons';
import { useMemo, isValidElement } from 'react';
import { classNames } from '../../../helpers/classnames.js';
import { IconButton } from '../../IconButton/IconButton.js';
import { Typography } from '../../Typography/Typography.js';

const ExpansionPanelTitle = ({ open = false, title, subTitle, description, iconName = Icons.NavArrowDown, customButton, className, ...props }) => {
    const titleTag = useMemo(() => {
        if (typeof title === 'string') {
            return (jsx(Typography, { variant: "subtitle1", className: "dsb_title-text", children: title }));
        }
        return jsx(Fragment, { children: title });
    }, [title]);
    const subTitleTag = useMemo(() => isValidElement(subTitle) ? (subTitle) : (jsx(Typography, { variant: "body3", className: "dsb_subtitle-text", children: subTitle })), [subTitle]);
    const descriptionTag = useMemo(() => isValidElement(description) ? (description) : (jsx(Typography, { variant: "body3", className: "dsb_description-text", children: description })), [description]);
    return (jsxs("div", { "data-testid": "ExpansionPanelTitle", className: classNames('dsb_expansion-panel-title', className), ...props, children: [jsxs("div", { className: "dsb_title-left-part", children: [title && titleTag, subTitle && subTitleTag] }), description && jsx("div", { className: "dsb_title-right-part", children: descriptionTag }), jsx("div", { className: "dsb_title-button-part", children: customButton || (jsx(IconButton, { "aria-expanded": true, size: "large", iconName: iconName, className: classNames('dsb_expansion-icon-button', open && 'dsb_expansion-icon-button__rotated') })) })] }));
};

export { ExpansionPanelTitle };
//# sourceMappingURL=ExpansionPanelTitle.js.map
