import { jsxs, jsx } from 'react/jsx-runtime';
import './ExpansionPanel2.js';
import { useState, useEffect } from 'react';
import { classNames } from '../../helpers/classnames.js';
import { ExpansionPanelTitle } from './ExpansionPanelTitle/ExpansionPanelTitle.js';

const ExpansionPanel = ({ open, title, description, subTitle, onOpen, onClose, className, titleClassName, bodyClassName, children, iconName, customButton, ...props }) => {
    const [_open, _setOpen] = useState(open);
    useEffect(() => {
        if (_open !== open) {
            _setOpen(open);
        }
    }, [open]);
    const handleClick = (event) => {
        if (_open) {
            onClose?.(event);
        }
        else {
            onOpen?.(event);
        }
        _setOpen(prev => !prev);
    };
    return (jsxs("section", { ...props, className: classNames('dsb_expansion-panel', className), "data-testid": "ExpansionPanel", children: [jsx(ExpansionPanelTitle, { open: _open, title: title, onClick: handleClick, subTitle: subTitle, description: description, iconName: iconName, customButton: customButton, className: classNames(titleClassName) }), jsx("div", { className: classNames('dsb_expansion-panel-body', !_open && 'dsb_expansion-panel-body__collapsed', bodyClassName), children: children })] }));
};

export { ExpansionPanel };
//# sourceMappingURL=ExpansionPanel.js.map
