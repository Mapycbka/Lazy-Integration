import { jsx } from 'react/jsx-runtime';
import './NavigationRail2.js';
import { Divider } from '../Divider/Divider.js';
import { Icon } from '../Icon/Icon.js';
import { classNames } from '../../helpers/classnames.js';

const NavigationRailIcon = ({ iconName, onClick, selected, disabled }) => (jsx("div", { onClick: disabled ? () => null : onClick, className: classNames('dsb_navigationRail__icon', selected && 'dsb_navigationRail__icon-selected', disabled && 'dsb_navigationRail__icon-disabled'), tabIndex: 0, children: jsx(Icon, { iconName: iconName, size: 'large' }) }));
const NavigationRail = ({ value, className, dataTestId = 'NavigationRail' }) => (jsx("div", { "data-testid": dataTestId, className: classNames('dsb_navigationRail', className), children: value.map(icon => {
        if (icon?.divider) {
            return jsx(Divider, {});
        }
        return (jsx(NavigationRailIcon, { disabled: icon.disabled, selected: icon.selected, iconName: icon.iconName, onClick: icon.onClick }));
    }) }));

export { NavigationRail };
//# sourceMappingURL=NavigationRail.js.map
