import { jsxs, jsx } from 'react/jsx-runtime';
import './Label2.js';
import { forwardRef } from 'react';
import { classNames } from '../../helpers/classnames.js';
import { Icon } from '../Icon/Icon.js';
import { Typography } from '../Typography/Typography.js';

const Label = forwardRef(({ type = 'default', assign, variant = 'outline', title, iconName, className, ...props }, ref) => {
    // Обратная совместимость, атрибут variant как в Material UI
    if (assign) {
        console.warn('В компоненте Label вместо параметра assing используйте variant, assing в будущем будет удален.');
    }
    const optionedVariant = assign || variant;
    return (jsxs("div", { "data-testid": "Label", ...props, className: classNames('dsb_label', `dsb_label__${optionedVariant}`, `dsb_label__${type}`, iconName && 'dsb_label__with-icon', !title && 'dsb_label__without-title', className), ref: ref, children: [iconName && jsx(Icon, { className: "dsb_label-icon", iconName: iconName, size: "medium" }), optionedVariant !== 'icon' && title && (jsx(Typography, { className: "dsb_label-title", variant: "subtitle3", children: title }))] }));
});

export { Label };
//# sourceMappingURL=Label.js.map
