import { jsx } from 'react/jsx-runtime';
import './Dropdown2.js';
import { NewDropdownMenu } from '../NewDropdownMenu/NewDropdownMenu.js';
import { useCallback } from 'react';
import { classNames } from '../../helpers/classnames.js';

const Dropdown = ({ anchorRef, open, onOpen, onClose, onOutsideClick, placement = 'bottom-start', verticalOffset = 1, horizontalOffset = 0, widthMode = 'widthParent', shouldRenderOverlay, overlayClassName, overlayScroll, scrollable, minHeight, applicationRootElementID, dropdownElementID, className, children, dataTestId = 'Dropdown', }) => {
    const handleSetOpen = useCallback((nextOpen) => {
        if (nextOpen) {
            onOpen?.();
        }
        else {
            onClose?.();
        }
    }, [onOpen, onClose]);
    if (!open) {
        return null;
    }
    const menuProps = {
        targetElement: anchorRef,
        isOpen: open,
        handleSetOpen,
        align: placement,
        verticalOffset,
        horizontalOffset,
        widthMode,
        shouldRenderOverlay,
        overlayClassName,
        overlayScroll,
        scrollable,
        minHeight,
        applicationRootElementID,
        dropdownElementID,
        onOutsideClick,
        dataTestId,
        className: classNames('dsb_dropdown', className),
    };
    return jsx(NewDropdownMenu, { ...menuProps, children: children });
};

export { Dropdown };
//# sourceMappingURL=Dropdown.js.map
