import { jsxs, Fragment, jsx } from 'react/jsx-runtime';
import './Header2.js';
import { Divider } from '../Divider/Divider.js';
import { Search } from '../Search/Search.js';
import { Typography } from '../Typography/Typography.js';
import { logosImgList } from '../../constants/logosImgLists.js';
import { useDeviceSize } from '../../helpers/use-device-size.js';
import { colorTextInactive } from '@beeline/design-tokens/js/tokens/globals/colors';
import React__default from 'react';

const WrappedElement = (element, wrapper) => (wrapper ? React__default.cloneElement(wrapper, {}, element) : element);
const Header = ({ nameProduct, nameLogo, customLogo, children, iconsList, searchComponentProps, nameProductRoutingComponent, nameLogoRoutingComponent, }) => {
    const { isExtraSmallDevice, isSmallDevice, isMediumDevice, isLargeDevice, isExtraLargeDevice, isExtraExtraLargeDevice, } = useDeviceSize();
    const defaultLogo = isExtraSmallDevice || isSmallDevice || isMediumDevice ? logosImgList['onlyLogo'] : logosImgList[nameLogo];
    // todo временный костыль на разные иконки
    const nameProductClass = ['logoDarkThemeSrc', 'logoLightThemeSrc', 'logoThemeable'].includes(nameLogo)
        ? 'dsb_header__product-name'
        : '';
    const logo = customLogo ?? (jsxs(Fragment, { children: [nameProduct &&
                (isLargeDevice || isExtraLargeDevice || isExtraExtraLargeDevice) &&
                // todo поменять после внедрения в типографию функционала
                WrappedElement(jsx(Typography, { className: nameProductClass, variant: "productName", style: { color: colorTextInactive }, children: nameProduct }), nameProductRoutingComponent), WrappedElement(nameLogo && defaultLogo, nameLogoRoutingComponent)] }));
    return (jsxs("header", { "data-testid": "Header", className: "dsb_header", role: "banner", children: [jsxs("div", { className: "dsb_header__container", children: [jsx("div", { className: "dsb_header__container-left", children: logo }), jsx("div", { className: "dsb_header__container-center", children: children }), jsxs("div", { className: "dsb_header__container-right", children: [searchComponentProps && jsx(Search, { ...searchComponentProps, isAdaptive: true }), iconsList] })] }), jsx(Divider, {})] }));
};

export { Header };
//# sourceMappingURL=Header.js.map
