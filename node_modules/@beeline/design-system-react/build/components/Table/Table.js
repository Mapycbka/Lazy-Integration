import { jsx } from 'react/jsx-runtime';
import './Table2.js';
import { classNames } from '../../helpers/classnames.js';

const Table = ({ dense = false, hover = false, alignRight = false, horizontalAlign, verticalAlign, numeric = false, monospaceNumeric = false, className, expandable, children, scroll, ...props }) => {
    const wrapperStyle = {};
    const tableStyle = {};
    if (scroll?.x !== undefined) {
        wrapperStyle.overflowX = 'auto';
        const x = scroll.x;
        if (typeof x === 'number') {
            wrapperStyle.width = x;
        }
        else if (x === true) {
            // Включение горизонтальной прокрутки при переполнении содержимого
            wrapperStyle.width = '100%';
        }
        else if (typeof x === 'string') {
            if (x === 'max-content') {
                tableStyle.width = 'max-content';
                wrapperStyle.width = '100%';
            }
            else {
                wrapperStyle.width = x;
            }
        }
        // Убедитесь, что стол может переполниться горизонтально, когда это необходимо
        if (!tableStyle.width && (typeof scroll.x === 'number' || scroll.x === true)) {
            tableStyle.width = 'max-content';
        }
    }
    if (scroll?.y !== undefined) {
        wrapperStyle.overflowY = 'auto';
        const y = scroll.y;
        if (typeof y === 'number') {
            wrapperStyle.maxHeight = y;
        }
        else if (typeof y === 'string') {
            wrapperStyle.maxHeight = y;
        }
    }
    const { style, ...restProps } = props;
    const tableElement = (jsx("table", { "data-testid": "Table", ...restProps, style: { ...style, ...tableStyle }, className: classNames('dsb_table', dense && 'dsb_table__dense', hover && 'dsb_table__hoverable', alignRight && 'dsb_table__align-right', horizontalAlign && `dsb_table__horizontal-align-${horizontalAlign}`, verticalAlign && `dsb_table__vertical-align-${verticalAlign}`, expandable && 'dsb_table__expandable', numeric && 'dsb_table__align-right', monospaceNumeric && 'dsb_table__monospace-numeric', className), children: children }));
    if (scroll) {
        return (jsx("div", { className: "dsb_table__scroll-wrapper", style: wrapperStyle, children: tableElement }));
    }
    return tableElement;
};

export { Table };
//# sourceMappingURL=Table.js.map
