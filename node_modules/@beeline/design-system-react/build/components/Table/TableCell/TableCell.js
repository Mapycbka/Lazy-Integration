import { jsx } from 'react/jsx-runtime';
import './TableCell2.js';
import { Icon } from '../../Icon/Icon.js';
import { Icons } from '@beeline/design-tokens/js/iconfont/icons';

/**
 * @name TableCell
 * @prop {ReactEventHandler} onClick нажаие по кнопке
 * @prop {isExpanded} isExpanded состояния открыта/закрыта
 */
const TableCell = ({ onClick, isExpanded }) => (jsx("div", { className: "dsb_table-cell__icon", "data-testid": "TableCell", children: jsx(Icon, { size: "small", onClick: onClick, iconName: isExpanded ? Icons.NavArrowUp : Icons.NavArrowDown }) }));

export { TableCell };
//# sourceMappingURL=TableCell.js.map
