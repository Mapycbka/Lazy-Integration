import { jsxs, Fragment, jsx } from 'react/jsx-runtime';
import { useState } from 'react';
import { IconButton } from '../IconButton/IconButton.js';

const Controlled = ({ inputRef, onChange, dataTestId, dataTestIconId, iconButton, maskConfig, ...props }) => {
    const [value, setValue] = useState('');
    const { onClick, ...rest } = iconButton;
    const handleChange = (event) => {
        setValue(event.target.value);
        onChange?.(event);
    };
    const handleClick = (event) => {
        onClick?.(event);
        setValue('');
    };
    return (jsxs(Fragment, { children: [jsx("input", { ref: inputRef, value: value, "data-testid": dataTestId, onChange: handleChange, ...props }), value && jsx(IconButton, { ...rest, dataTestId: dataTestIconId, onClick: handleClick })] }));
};

export { Controlled };
//# sourceMappingURL=Controlled.js.map
