import { jsx } from 'react/jsx-runtime';
import './Box2.js';
import { classNames } from '../../helpers/classnames.js';

const Box = ({ as: Component = 'div', display = 'block', padding, paddingX, paddingY, margin, marginX, marginY, fullWidth, className, dataTestId = 'Box', ...rest }) => {
    const ComponentTag = Component;
    const boxClassName = classNames('dsb_box', `dsb_box__display-${display}`, fullWidth && 'dsb_box__full-width', padding && `dsb_box__padding-${padding}`, paddingX && `dsb_box__padding-x-${paddingX}`, paddingY && `dsb_box__padding-y-${paddingY}`, margin && `dsb_box__margin-${margin}`, marginX && `dsb_box__margin-x-${marginX}`, marginY && `dsb_box__margin-y-${marginY}`, className);
    return jsx(ComponentTag, { "data-testid": dataTestId, className: boxClassName, ...rest });
};
Box.displayName = 'Box';

export { Box };
//# sourceMappingURL=Box.js.map
