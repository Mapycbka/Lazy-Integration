import { jsx, jsxs } from 'react/jsx-runtime';
import './AvatarText2.js';
import { classNames } from '../../helpers/classnames.js';
import { Avatar } from '../Avatar/Avatar.js';
import { Informer } from '../Informer/Informer.js';
import { Typography } from '../Typography/Typography.js';

const AvatarText = ({ size = 'default', avatarProps, informerProps, label, description, suffix, className, dataTestId = 'AvatarText', ...props }) => {
    // Определяем размер Avatar в зависимости от size компонента
    const avatarSize = size === 'default' ? 'medium' : 'small';
    // Определяем размер Informer в зависимости от size компонента
    // default → 'large', mini → 'medium'
    const informerSize = informerProps?.size || (size === 'default' ? 'large' : 'medium');
    // Создаем Avatar с правильным размером
    const avatar = (jsx(Avatar, { ...avatarProps, size: avatarSize, className: classNames('dsb_avatar-text__avatar', avatarProps.className) }));
    // Оборачиваем Avatar в Informer, если передан informerProps
    const avatarWithInformer = informerProps ? (jsx(Informer, { ...informerProps, size: informerSize, className: classNames('dsb_avatar-text__informer', informerProps.className), children: avatar })) : (avatar);
    return (jsxs("div", { ...props, "data-testid": dataTestId, className: classNames('dsb_avatar-text', `dsb_avatar-text--${size}`, className), children: [avatarWithInformer, jsxs("div", { className: "dsb_avatar-text__content", children: [jsxs("div", { className: "dsb_avatar-text__top-row", children: [jsx(Typography, { variant: "body2", ellipsis: true, className: "dsb_avatar-text__label", children: label }), suffix && size !== 'mini' && (jsx("div", { className: "dsb_avatar-text__suffix", children: typeof suffix === 'string' ? (jsx(Typography, { variant: "body3", ellipsis: true, className: "dsb_avatar-text__suffix-text", children: suffix })) : (suffix) }))] }), description && size !== 'mini' && (jsx(Typography, { variant: "body3", inactive: true, className: "dsb_avatar-text__description", children: description }))] })] }));
};

export { AvatarText };
//# sourceMappingURL=AvatarText.js.map
