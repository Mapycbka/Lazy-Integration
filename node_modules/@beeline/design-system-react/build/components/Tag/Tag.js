import { jsxs, jsx } from 'react/jsx-runtime';
import './Tag2.js';
import { IconButton } from '../IconButton/IconButton.js';
import { classNames } from '../../helpers/classnames.js';
import { Icons } from '@beeline/design-tokens/js/iconfont';
import { Typography } from '../Typography/Typography.js';

const Tag = ({ label, onRemove, onClick, removeAriaLabel, className, dataTestId = 'Tag', ...rest }) => {
    const handleClick = (event) => {
        onClick?.(event);
    };
    const handleRemoveClick = (event) => {
        event.stopPropagation();
        onRemove?.();
    };
    const generatedRemoveAriaLabel = typeof label === 'string' ? `Удалить тег ${label}` : 'Удалить тег';
    return (jsxs("div", { ...rest, "data-testid": dataTestId, onClick: onClick ? handleClick : undefined, className: classNames('dsb_tag', className), children: [jsx(Typography, { variant: "body3", className: "dsb_tag-label", children: label }), jsx(IconButton, { size: "medium", iconName: Icons.Close, className: "dsb_tag-close-icon", onClick: handleRemoveClick, "aria-label": removeAriaLabel ?? generatedRemoveAriaLabel })] }));
};

export { Tag };
//# sourceMappingURL=Tag.js.map
