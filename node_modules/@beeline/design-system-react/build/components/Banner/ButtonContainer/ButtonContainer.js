import { jsx } from 'react/jsx-runtime';
import './ButtonContainer2.js';
import { classNames } from '../../../helpers/classnames.js';
import { Button } from '../../Button/Button.js';
import '../../Button/ProgressButton/ProgressButton.js';

const ButtonContainer = ({ actions, className }) => (jsx("div", { "data-testid": "ButtonContainer", className: classNames('banner-button-container', className), children: actions.map(({ startIcon, label, onClick, variant }) => (jsx(Button, { startIcon: startIcon, size: "small", variant: variant ?? 'overlay', onClick: onClick, className: "banner-button", children: label }, label))) }));

export { ButtonContainer };
//# sourceMappingURL=ButtonContainer.js.map
