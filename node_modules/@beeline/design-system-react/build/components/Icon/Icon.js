import { jsx } from 'react/jsx-runtime';
import './Icon2.js';
import { forwardRef } from 'react';
import { classNames } from '../../helpers/classnames.js';
import { useColorInteadOfStatus } from '../../types/status.js';

const Icon = forwardRef(({ iconName, size = 'medium', type, className, contained, color, dataTestId = 'Icon', ...props }, ref) => {
    // TODO: удалить при удалении пропса type (не забудь указать пропсу color дефолтное значение)
    // при удалении заменить использование iconColor на color
    const iconColor = useColorInteadOfStatus({ color, type }, 'Icon');
    // TODO: удалить при удалении пропса type
    // при удалении заменить использование isContained на contained
    const isContained = contained || Boolean(type);
    return (jsx("span", { ...props, "data-testid": dataTestId, role: "img", className: classNames('beeline-icons', 'dsb_icon', isContained && 'dsb_icon--contained', isContained && `dsb_icon--${iconColor}`, !isContained && `dsb_icon--${size}`, !isContained && `dsb_icon--sized--${iconColor}`, className), translate: "no", ref: ref, children: iconName }));
});

export { Icon };
//# sourceMappingURL=Icon.js.map
