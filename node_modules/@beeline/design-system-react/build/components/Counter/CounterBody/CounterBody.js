import { jsxs, jsx } from 'react/jsx-runtime';
import './CounterBody2.js';
import { forwardRef } from 'react';
import { classNames } from '../../../helpers/classnames.js';
import { BadgeContent } from '../BadgeContent/BadgeContent.js';
import { isNumber } from '../utils.js';

const CounterBody = forwardRef(({ children, className, count, warning, hideBadgeContent, error, size, ...rest }, ref) => {
    const rootClassName = classNames('dsb-counter-root', className);
    return (jsxs("div", { "data-testid": "CounterBody", ...rest, className: rootClassName, ref: ref, children: [children, (isNumber(count) || warning) && (jsx(BadgeContent, { count: count, error: error, warning: warning, hideBadgeContent: hideBadgeContent, size: size, stanalone: !children }))] }));
});

export { CounterBody };
//# sourceMappingURL=CounterBody.js.map
