import { jsx } from 'react/jsx-runtime';
import './Stepper2.js';
import { Icon } from '../../../../Icon/Icon.js';
import { classNames } from '../../../../../helpers/classnames.js';
import { Icons } from '@beeline/design-tokens/js/iconfont/icons';

const Stepper = ({ content, activeSlideIndex = 0, onPaginationChange }) => {
    const handleStep = (index) => () => {
        onPaginationChange?.(index);
    };
    return (jsx("div", { className: "dsb_onboarding_stepper", children: content.map((_contentItem, index) => (jsx(Icon, { size: "small", iconName: Icons.Dot, onClick: handleStep(index), className: classNames('dsb_onboarding_stepper-step', index > 0 && 'dsb_onboarding_stepper-step__chained', index === activeSlideIndex && 'dsb_onboarding_stepper-step__active') }, index))) }));
};

export { Stepper };
//# sourceMappingURL=Stepper.js.map
