const range = (start, end, step = 1) => {
    const arr = [];
    const arrayStep = step === 0 || typeof step === 'string' ? 1 : step;
    for (let i = start; i <= end; i += arrayStep) {
        arr.push(i);
    }
    return arr;
};

export { range };
//# sourceMappingURL=range.js.map
