import { jsx } from 'react/jsx-runtime';
import './List2.js';
import { useMemo } from 'react';
import { classNames } from '../../helpers/classnames.js';

const List = ({ className, type = 'ul', ...props }) => {
    const classes = useMemo(() => classNames('dsb_list', className), [className]);
    return type === 'ul' ? (jsx("ul", { "data-testid": "List", ...props, className: classes })) : (jsx("ol", { "data-testid": "List", ...props, className: classes }));
};

export { List };
//# sourceMappingURL=List.js.map
