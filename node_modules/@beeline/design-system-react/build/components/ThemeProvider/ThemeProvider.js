import { jsx } from 'react/jsx-runtime';
import './ThemeProvider2.js';
import { useState, useEffect } from 'react';
import { classNames } from '../../helpers/classnames.js';

const ThemeProvider = ({ children, isRoot = false, theme = 'light', rootSelector = 'body', className = '', ...rest }) => {
    const [themeClass, setThemeClass] = useState('ds-theme--light');
    useEffect(() => {
        const rootEl = document.querySelector(rootSelector);
        const newThemeClassName = theme === 'light' ? 'ds-theme--light' : 'ds-theme--dark';
        const oldThemeClassName = themeClass;
        if (isRoot) {
            rootEl.classList.remove(oldThemeClassName);
            rootEl.classList.add(newThemeClassName);
        }
        setThemeClass(newThemeClassName);
    }, [theme, isRoot, rootSelector, themeClass]);
    return (jsx("div", { "data-testid": "ThemeProvider", className: classNames(!isRoot && themeClass, className), ...rest, children: children }));
};

export { ThemeProvider };
//# sourceMappingURL=ThemeProvider.js.map
