import { jsx } from 'react/jsx-runtime';
import React__default from 'react';
import { FlagIconsByCode } from '../../Flags/index.js';

const FlagComponent = ({ value }) => {
    const Icon = FlagIconsByCode[value.nameCode];
    if (!Icon) {
        return null;
    }
    return jsx(Icon, { width: 20, height: 20, role: "img", "aria-label": value.nameCode, focusable: "false" });
};
const Flag = React__default.memo(FlagComponent);

export { Flag };
//# sourceMappingURL=Flag.js.map
