import { jsxs, jsx } from 'react/jsx-runtime';
import './TimePicker2.js';
import { Icons } from '@beeline/design-tokens/js/iconfont/icons';
import { forwardRef, useState } from 'react';
import { classNames } from '../../helpers/classnames.js';
import { Icon } from '../Icon/Icon.js';
import { TextField } from '../TextField/TextField.js';

const TimePicker = forwardRef(({ className, hidePicker = true, showPlaceholder = true, showIcon = true, disabled = false, dataTestId = 'TimePicker', step = '60', onChange, error = false, fullWidth, defaultValue = '', ...restProps }, forwardedRef) => {
    const [inputValue, setInputValue] = useState(defaultValue);
    const [isFocused, setIsFocused] = useState(false);
    const handleInputChange = (event) => {
        setInputValue(event.target.value);
        onChange?.(event.target.value);
    };
    const handleInputFocus = (event) => {
        setIsFocused(true);
    };
    const handleInputBlur = (event) => {
        setIsFocused(false);
    };
    return (jsxs("div", { "data-testid": dataTestId, className: classNames('dsb_timepicker', fullWidth && 'dsb_timepicker__full-width', className), children: [showIcon && jsx(Icon, { iconName: Icons.Clock, className: "dsb_timepicker__clock-icon" }), jsx(TextField, { ref: forwardedRef, className: classNames('dsb_timepicker__input', hidePicker && 'dsb_timepicker__input--hide-picker', showPlaceholder && !isFocused && !inputValue && !error && 'dsb_timepicker__input--placeholder', fullWidth && 'dsb_timepicker__input--full-width', className), value: inputValue, onBlur: handleInputBlur, onFocus: handleInputFocus, onChange: handleInputChange, type: "time", step: step, required: true, disabled: disabled, error: error, ...restProps })] }));
});
TimePicker.displayName = 'TimePicker';

export { TimePicker };
//# sourceMappingURL=TimePicker.js.map
