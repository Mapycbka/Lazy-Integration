import { jsx } from 'react/jsx-runtime';
import { PANELS } from '../../constants.js';
import { CalendarDayView } from '../../views/CalendarDayView/CalendarDayView.js';
import { CalendarMonthView } from '../../views/CalendarMonthView/CalendarMonthView.js';
import { CalendarYearView } from '../../views/CalendarYearView/CalendarYearView.js';

const CalendarPanel = ({ panel, minDate, maxDate, viewDate, disabledStartDate, disabledEndDate, selectedDate, // одиночная или массив
selectDay, multiselect, readonly, excludeDate, holidaysDate, setDaysPanel, setMonthsPanel, selectMonth, selectYear, timeZone, customizeDay, showAdjacentMonthDays, allowAdjacentMonthSelection, }) => {
    switch (panel) {
        case PANELS.DAYS: {
            return (jsx(CalendarDayView, { minDate: minDate, maxDate: maxDate, viewDate: viewDate, disabledStartDate: disabledStartDate, disabledEndDate: disabledEndDate, selectedDate: selectedDate, onSelectDay: selectDay, multiselect: multiselect, readonly: readonly, excludeDate: excludeDate, holidaysDate: holidaysDate, customizeDay: customizeDay, timeZone: timeZone, showAdjacentMonthDays: showAdjacentMonthDays, allowAdjacentMonthSelection: allowAdjacentMonthSelection }));
        }
        case PANELS.MONTHS: {
            return (jsx(CalendarMonthView, { minDate: minDate, maxDate: maxDate, viewDate: viewDate, showStartPanel: setDaysPanel, onSelectMonth: selectMonth, timeZone: timeZone }));
        }
        case PANELS.YEARS: {
            return (jsx(CalendarYearView, { viewDate: viewDate, selectedYear: viewDate.getFullYear(), minYear: minDate?.getFullYear(), maxYear: maxDate?.getFullYear(), onSelectYear: selectYear, showStartPanel: setMonthsPanel, timeZone: timeZone }));
        }
        default: {
            return null;
        }
    }
};

export { CalendarPanel };
//# sourceMappingURL=CalendarPanel.js.map
