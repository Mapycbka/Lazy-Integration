import { jsx } from 'react/jsx-runtime';
import './CalendarNavigationIcon2.js';
import { classNames } from '../../../../helpers/classnames.js';
import { Icon } from '../../../Icon/Icon.js';

const CalendarNavigationIcon = ({ className: customClassName, direction, onClick, disabled = false, dataTestId, }) => {
    const className = classNames('dsb_calendar_icon', customClassName, {
        disabled,
    });
    const onClickHandler = disabled ? undefined : onClick;
    return (jsx(Icon, { iconName: direction, className: className, onClick: onClickHandler, size: "large", dataTestId: dataTestId }));
};

export { CalendarNavigationIcon };
//# sourceMappingURL=CalendarNavigationIcon.js.map
