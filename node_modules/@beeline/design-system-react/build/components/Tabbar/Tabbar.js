import { jsx } from 'react/jsx-runtime';
import './Tabbar2.js';
import { classNames } from '../../helpers/classnames.js';
import React__default, { isValidElement } from 'react';
import { TabbarItem } from './TabbarItem/TabbarItem.js';

const Tabbar = ({ className, type = 'default', children, ...rest }) => {
    const provideTypeProp = element => isValidElement(element) && element.type === TabbarItem
        ? React__default.cloneElement(element, { type })
        : element;
    return (jsx("nav", { "data-testid": "Tabbar", className: classNames('dsb_tabbar', `dsb_tabbar-${type}`, className), ...rest, children: jsx("ul", { className: "dsb_tabbar__items", children: Array.isArray(children) ? children.map(child => provideTypeProp(child)) : provideTypeProp(children) }) }));
};

export { Tabbar };
//# sourceMappingURL=Tabbar.js.map
