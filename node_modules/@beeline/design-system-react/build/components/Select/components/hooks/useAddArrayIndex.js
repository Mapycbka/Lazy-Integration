import { shallowEqual } from '../../../../helpers/shallowEqual.js';
import { useMemo } from 'react';

// TODO надо придумать адекватное название
const useAddArrayIndex = ({ options = [], values = [] }) => {
    // добавляем опциям айдишники, пользователю они ни к чему
    const privateOptions = useMemo(() => options?.map((option, index) => ({
        id: index,
        value: option,
        disabled: option?.disabled,
    })), [options]);
    // добавляем выбранным значениям айдишники, пользователю они ни к чему
    const privateValues = useMemo(() => values.map(value => {
        const id = privateOptions?.find(opt => shallowEqual(opt.value, value))?.id;
        return {
            id,
            value,
        };
    }), [values, privateOptions]);
    return [privateOptions, privateValues];
};

export { useAddArrayIndex };
//# sourceMappingURL=useAddArrayIndex.js.map
