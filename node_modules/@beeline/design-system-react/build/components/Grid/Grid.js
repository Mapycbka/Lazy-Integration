import { jsx } from 'react/jsx-runtime';
import './Grid2.js';
import { Box } from '../Box/Box.js';
import { classNames } from '../../helpers/classnames.js';

const Grid = ({ as, columns = 1, gap = 'none', columnGap, rowGap, align = 'stretch', justify = 'stretch', className, dataTestId = 'Grid', ...rest }) => {
    const gridClassName = classNames('dsb_grid', `dsb_grid__columns-${columns}`, gap && `dsb_grid__gap-${gap}`, columnGap && `dsb_grid__column-gap-${columnGap}`, rowGap && `dsb_grid__row-gap-${rowGap}`, align && `dsb_grid__align-${align}`, justify && `dsb_grid__justify-${justify}`, className);
    return jsx(Box, { as: as, display: "grid", className: gridClassName, dataTestId: dataTestId, ...rest });
};
Grid.displayName = 'Grid';

export { Grid };
//# sourceMappingURL=Grid.js.map
