import { jsxs, jsx, Fragment } from 'react/jsx-runtime';
import React__default from 'react';
import './HeroBanner2.js';
import { Button } from '../Button/Button.js';
import '../Button/ProgressButton/ProgressButton.js';
import { Typography } from '../Typography/Typography.js';
import { classNames } from '../../helpers/classnames.js';

const HeroBanner = ({ header, headline, imagePosition = 'absolute', image, actions, renderAction, onAction, dataTestId = 'HeroBanner', className, imageAlt, ...rest }) => {
    const base = 'dsb_herobanner';
    const handleClick = (action) => () => {
        onAction?.(action);
    };
    const renderImage = () => {
        if (!image)
            return null;
        return (jsx(Fragment, { children: typeof image === 'string' ? (jsx("img", { className: classNames(`${base}__image`, `${base}__image-${imagePosition}`), src: image, alt: imageAlt })) : (image) }));
    };
    const renderActions = () => {
        if (!actions?.length)
            return null;
        return (jsx("div", { className: `${base}__actions`, children: actions.map(a => renderAction ? (jsx(React__default.Fragment, { children: renderAction(a, handleClick(a)) })) : (jsx(Button, { href: a.href, size: 'medium', variant: a.primary ? 'contained' : 'outlined', onClick: handleClick(a), disabled: a.disabled, "aria-label": a['aria-label'], children: a.label }))) }));
    };
    return (jsxs("div", { className: classNames(base, className), "data-testId": dataTestId, role: "banner", ...rest, children: [jsxs("div", { className: `${base}-content`, children: [header && (jsx(Typography, { variant: "h1", className: `${base}__header`, children: header })), headline && (jsx(Typography, { variant: "body1", inactive: true, className: `${base}__headline`, children: headline })), renderActions()] }), renderImage()] }));
};

export { HeroBanner, HeroBanner as default };
//# sourceMappingURL=HeroBanner.js.map
