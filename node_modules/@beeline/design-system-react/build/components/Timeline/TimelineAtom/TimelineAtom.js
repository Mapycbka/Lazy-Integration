import { jsx } from 'react/jsx-runtime';
import './TimelineAtom2.js';
import { Icons } from '@beeline/design-tokens/js/iconfont/icons';
import { useMemo } from 'react';
import { classNames } from '../../../helpers/classnames.js';
import { Icon } from '../../Icon/Icon.js';

const TimelineAtom = ({ type = 'current', position = 'top', direction = 'vertical', iconName, collapsable = false, collapsed = false, }) => {
    /** Возвращает иконку для Атома(кружочек) активного шага:
     * Если шаг с ошибкой: иконка "крестик"
     * Если шаг последний: иконка "галочка"
     * Если шаг непоследний: иконка "шеврон вниз"
     * */
    const getAtomIconName = useMemo(() => {
        if (iconName) {
            return iconName;
        }
        if (type === 'error') {
            return Icons.Close;
        }
        if (position === 'bottom') {
            return Icons.Check;
        }
        return Icons.FastArrowDown;
    }, [iconName, type, position]);
    const atomClasses = classNames('dsb_atom', `dsb_atom__${direction}`, `dsb_atom__${position}`);
    const contentClasses = classNames('dsb_atom-content', collapsable && collapsed && 'dsb_atom-content__collapsed', `dsb_atom-content__${position}`, `dsb_atom-content__${type}`, `dsb_atom-content__${direction}`);
    return (jsx("div", { "data-testid": "TimelineAtom", className: atomClasses, children: jsx("div", { className: contentClasses, children: jsx("div", { className: classNames('dsb_atom-icon', `dsb_atom-icon__${type}`), children: (type === 'current' || type === 'error') && jsx(Icon, { size: "medium", iconName: getAtomIconName }) }) }) }));
};

export { TimelineAtom };
//# sourceMappingURL=TimelineAtom.js.map
