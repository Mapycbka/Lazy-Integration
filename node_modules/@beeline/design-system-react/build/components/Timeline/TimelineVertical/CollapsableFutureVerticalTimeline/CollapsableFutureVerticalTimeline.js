import { jsx, Fragment } from 'react/jsx-runtime';
import './CollapsableFutureVerticalTimeline2.js';
import { calcStepPosition } from '../../utils.js';
import { VerticalContent } from '../VerticalContent/VerticalContent.js';

const CollapsableFutureVerticalTimeline = ({ steps }) => {
    const stepsLength = steps.length;
    if (!stepsLength) {
        return null;
    }
    return (jsx(Fragment, { children: steps.map((step, index) => {
            const position = calcStepPosition(index, stepsLength - 1);
            const type = 'inactive';
            return (jsx("div", { className: "dsb_timeline-future-step", children: jsx(VerticalContent, { ...step, type: type, position: position }) }, step.id));
        }) }));
};

export { CollapsableFutureVerticalTimeline };
//# sourceMappingURL=CollapsableFutureVerticalTimeline.js.map
