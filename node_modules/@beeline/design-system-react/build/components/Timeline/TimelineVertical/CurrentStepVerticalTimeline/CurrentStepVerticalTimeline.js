import { jsx } from 'react/jsx-runtime';
import './CurrentStepVerticalTimeline2.js';
import { useMemo } from 'react';
import { VerticalContent } from '../VerticalContent/VerticalContent.js';

const CurrentStepVerticalTimeline = ({ steps, completed, bottom = false, collapsable = false, collapsed = false, }) => {
    const [step] = steps;
    const type = useMemo(() => {
        if (completed) {
            return 'active';
        }
        else if (step?.error) {
            return 'error';
        }
        return 'current';
    }, [completed, step]);
    if (!step) {
        return null;
    }
    const position = completed || bottom ? 'bottom' : 'middle';
    return (jsx("div", { "data-testid": "CurrentStepVerticalTimeline", className: "dsb_timeline-current-step", children: jsx(VerticalContent, { ...step, type: type, position: position, collapsable: collapsable, collapsed: collapsed }) }, step.id));
};

export { CurrentStepVerticalTimeline };
//# sourceMappingURL=CurrentStepVerticalTimeline.js.map
