import { jsx, Fragment } from 'react/jsx-runtime';
import { createElement } from 'react';
import { VerticalContent } from '../VerticalContent/VerticalContent.js';

const CollapsableVerticalTimeline = ({ steps }) => {
    const stepsLength = steps.length;
    if (!stepsLength) {
        return null;
    }
    return (jsx(Fragment, { children: steps.map(step => (createElement(VerticalContent, { ...step, key: step.id, type: "active", position: "middle" }))) }));
};

export { CollapsableVerticalTimeline };
//# sourceMappingURL=CollapsableVerticalTimeline.js.map
