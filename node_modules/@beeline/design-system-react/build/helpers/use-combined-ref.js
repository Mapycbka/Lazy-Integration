import { useCallback } from 'react';

const useCombinedRef = (...refs) => {
    const refCb = useCallback((element) => {
        for (const ref of refs) {
            if (!ref) {
                continue;
            }
            if (typeof ref === 'function') {
                ref(element);
            }
            else {
                ref.current = element;
            }
        }
    }, [refs]);
    return refCb;
};

export { useCombinedRef };
//# sourceMappingURL=use-combined-ref.js.map
